### MODULE role score=33 len=41078

   "RoleImage","roleImage",
            "RoleImageUrl","roleImageUrl",
            "RoleBigImage","roleBigImage",
            "RolePoster","rolePoster",
            "ImageUrl","imageUrl",
            "RolePic","rolePic",
            "Picture","picture",
            "Photo","photo",
            "Portrait","portrait",
            "Avatar","avatar",
            "HeadImage","headImage"
        ];
        for(var i=0;i<ks.length;i++){
            var u=qfRoleNormImg2940(o[ks[i]]);
            if(u)return u;
        }
        return "";
    }

    var hit=direct(roleObj);
    if(hit)return hit;
    hit=direct(containerObj);
    if(hit)return hit;

    var best="",bestScore=-1,seen=[];
    function walk(node,depth){
        if(!node||depth>5||bestScore>=120)return;
        if(typeof node!=="object")return;
        if(seen.indexOf(node)>=0)return;
        seen.push(node);

        if(Array.isArray(node)){
            for(var ai=0;ai<node.length&&ai<24;ai++)walk(node[ai],depth+1);
            return;
        }

        for(var k in node){
            if(!Object.prototype.hasOwnProperty.call(node,k))continue;
            var v=node[k];

            if(typeof v==="string"){
                var u=qfRoleNormImg2940(v);
                if(!u)continue;

                var key=String(k||"").toLowerCase();
                var score=0;
                if(/role.*back.*ground/.test(key))score+=120;
                else if(/background/.test(key))score+=90;
                else if(/role.*image|role.*pic/.test(key))score+=75;
                else if(/portrait|photo/.test(key))score+=55;
                else if(/image|img|pic/.test(key))score+=40;
                else if(/avatar|head/.test(key))score+=20;

                if(/thumb|small|icon/.test(key))score-=35;
                if(/bookcover|cover/.test(u.toLowerCase()))score-=15;

                if(score>bestScore){
                    bestScore=score;
                    best=u;
                }
            }else if(v&&typeof v==="object"){
                walk(v,depth+1);
            }
        }
    }

    walk(roleObj,0);
    walk(containerObj,0);
    return best;
}

function qfRoleTags2940(roleObj,containerObj){
    var cand=[
        roleObj&&roleObj.TagList,roleObj&&roleObj.tagList,
        roleObj&&roleObj.Tags,roleObj&&roleObj.tags,
        roleObj&&roleObj.RoleTagList,roleObj&&roleObj.roleTagList,
        roleObj&&roleObj.CharacterTagList,roleObj&&roleObj.characterTagList,
        roleObj&&roleObj.HonorList,roleObj&&roleObj.honorList,
        roleObj&&roleObj.LabelList,roleObj&&roleObj.labelList,
        containerObj&&containerObj.TagList,containerObj&&containerObj.tagList,
        containerObj&&containerObj.RoleTagList,containerObj&&containerObj.roleTagList,
        containerObj&&containerObj.CharacterTagList,containerObj&&containerObj.characterTagList,
        containerObj&&containerObj.HonorList,containerObj&&containerObj.honorList,
        containerObj&&containerObj.LabelList,containerObj&&containerObj.labelList
    ];
    var out=[],seen={};
    for(var c=0;c<cand.length;c++){
        var a=qfRoleArr2940(cand[c]);
        for(var i=0;i<a.length&&out.length<24;i++){
            var t=a[i],name="",likes=0;
            if(typeof t==="string"){
                name=qfRolePlainB61(t);
            }else{
                t=t||{};
                var ti=t.TagInfo||t.tagInfo||t.LabelInfo||t.labelInfo||{};
                name=qfRolePlainB61(
                    t.TagName||t.tagName||t.Name||t.name||t.Title||t.title||
                    t.LabelName||t.labelName||ti.TagName||ti.tagName||ti.Name||ti.name||""
                );
                likes=qfRoleNum2943(
                    t.Likes||t.likes||t.LikeCount||t.likeCount||t.Count||t.count||
                    t.HotValue||t.hotValue||ti.Likes||ti.likes||ti.Count||ti.count||0
                );
            }
            if(!name||name.length>40||seen[name])continue;
            seen[name]=1;
            out.push({name:name,likes:likes||0});
        }
    }
    return out;
}

function qfRoleNum2943(v){
    if(v===undefined||v===null||v==="")return 0;
    if(typeof v==="number")return isFinite(v)?v:0;
    var s=String(v).replace(/,/g,"").trim();
    var m=s.match(/-?\d+(?:\.\d+)?/);
    return m?Number(m[0])||0:0;
}

function qfRolePrettyNum2943(n){
    n=Number(n||0);
    if(!isFinite(n)||n<=0)return "";
    if(n>=100000000){
        var y=n/100000000;
        return (Math.round(y*10)/10).toString().replace(/\.0$/,"")+"亿";
    }
    if(n>=10000){
        var w=n/10000;
        return (Math.round(w*10)/10).toString().replace(/\.0$/,"")+"万";
    }
    return String(Math.round(n));
}

function qfRoleNormalize2940(root,bid){
    var d=(root&&(root.Data||root.data))||root||{};
    var out=[],byId={},byName={};
    bid=String(bid||"");

    function cleanId(v){
        if(v===undefined||v===null)return "";
        var s=String(v).trim();
        if(!/^\d{5,}$/.test(s))return "";
        if(bid&&s===bid)return "";
        return s;
    }
    function directRoleId(o,allowGeneric){
        if(!o||typeof o!=="object")return "";
        var ks=[
            "RoleId","roleId","RoleID","roleID",
            "BookRoleId","bookRoleId","BookRoleID","bookRoleID",
            "CharacterId","characterId","CharacterID","characterID"
        ];
        for(var i=0;i<ks.length;i++){var x=cleanId(o[ks[i]]);if(x)return x;}
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var lk=String(k).toLowerCase();
            if(((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return

 var u=qfRoleNormImg2940(o[ks[i]]);
            if(u)return u;
        }
        return "";
    }

    var hit=direct(roleObj);
    if(hit)return hit;
    hit=direct(containerObj);
    if(hit)return hit;

    var best="",bestScore=-1,seen=[];
    function walk(node,depth){
        if(!node||depth>5||bestScore>=120)return;
        if(typeof node!=="object")return;
        if(seen.indexOf(node)>=0)return;
        seen.push(node);

        if(Array.isArray(node)){
            for(var ai=0;ai<node.length&&ai<24;ai++)walk(node[ai],depth+1);
            return;
        }

        for(var k in node){
            if(!Object.prototype.hasOwnProperty.call(node,k))continue;
            var v=node[k];

            if(typeof v==="string"){
                var u=qfRoleNormImg2940(v);
                if(!u)continue;

                var key=String(k||"").toLowerCase();
                var score=0;
                if(/role.*back.*ground/.test(key))score+=120;
                else if(/background/.test(key))score+=90;
                else if(/role.*image|role.*pic/.test(key))score+=75;
                else if(/portrait|photo/.test(key))score+=55;
                else if(/image|img|pic/.test(key))score+=40;
                else if(/avatar|head/.test(key))score+=20;

                if(/thumb|small|icon/.test(key))score-=35;
                if(/bookcover|cover/.test(u.toLowerCase()))score-=15;

                if(score>bestScore){
                    bestScore=score;
                    best=u;
                }
            }else if(v&&typeof v==="object"){
                walk(v,depth+1);
            }
        }
    }

    walk(roleObj,0);
    walk(containerObj,0);
    return best;
}

function qfRoleTags2940(roleObj,containerObj){
    var cand=[
        roleObj&&roleObj.TagList,roleObj&&roleObj.tagList,
        roleObj&&roleObj.Tags,roleObj&&roleObj.tags,
        roleObj&&roleObj.RoleTagList,roleObj&&roleObj.roleTagList,
        roleObj&&roleObj.CharacterTagList,roleObj&&roleObj.characterTagList,
        roleObj&&roleObj.HonorList,roleObj&&roleObj.honorList,
        roleObj&&roleObj.LabelList,roleObj&&roleObj.labelList,
        containerObj&&containerObj.TagList,containerObj&&containerObj.tagList,
        containerObj&&containerObj.RoleTagList,containerObj&&containerObj.roleTagList,
        containerObj&&containerObj.CharacterTagList,containerObj&&containerObj.characterTagList,
        containerObj&&containerObj.HonorList,containerObj&&containerObj.honorList,
        containerObj&&containerObj.LabelList,containerObj&&containerObj.labelList
    ];
    var out=[],seen={};
    for(var c=0;c<cand.length;c++){
        var a=qfRoleArr2940(cand[c]);
        for(var i=0;i<a.length&&out.length<24;i++){
            var t=a[i],name="",likes=0;
            if(typeof t==="string"){
                name=qfRolePlainB61(t);
            }else{
                t=t||{};
                var ti=t.TagInfo||t.tagInfo||t.LabelInfo||t.labelInfo||{};
                name=qfRolePlainB61(
                    t.TagName||t.tagName||t.Name||t.name||t.Title||t.title||
                    t.LabelName||t.labelName||ti.TagName||ti.tagName||ti.Name||ti.name||""
                );
                likes=qfRoleNum2943(
                    t.Likes||t.likes||t.LikeCount||t.likeCount||t.Count||t.count||
                    t.HotValue||t.hotValue||ti.Likes||ti.likes||ti.Count||ti.count||0
                );
            }
            if(!name||name.length>40||seen[name])continue;
            seen[name]=1;
            out.push({name:name,likes:likes||0});
        }
    }
    return out;
}

function qfRoleNum2943(v){
    if(v===undefined||v===null||v==="")return 0;
    if(typeof v==="number")return isFinite(v)?v:0;
    var s=String(v).replace(/,/g,"").trim();
    var m=s.match(/-?\d+(?:\.\d+)?/);
    return m?Number(m[0])||0:0;
}

function qfRolePrettyNum2943(n){
    n=Number(n||0);
    if(!isFinite(n)||n<=0)return "";
    if(n>=100000000){
        var y=n/100000000;
        return (Math.round(y*10)/10).toString().replace(/\.0$/,"")+"亿";
    }
    if(n>=10000){
        var w=n/10000;
        return (Math.round(w*10)/10).toString().replace(/\.0$/,"")+"万";
    }
    return String(Math.round(n));
}

function qfRoleNormalize2940(root,bid){
    var d=(root&&(root.Data||root.data))||root||{};
    var out=[],byId={},byName={};
    bid=String(bid||"");

    function cleanId(v){
        if(v===undefined||v===null)return "";
        var s=String(v).trim();
        if(!/^\d{5,}$/.test(s))return "";
        if(bid&&s===bid)return "";
        return s;
    }
    function directRoleId(o,allowGeneric){
        if(!o||typeof o!=="object")return "";
        var ks=[
            "RoleId","roleId","RoleID","roleID",
            "BookRoleId","bookRoleId","BookRoleID","bookRoleID",
            "CharacterId","characterId","CharacterID","characterID"
        ];
        for(var i=0;i<ks.length;i++){var x=cleanId(o[ks[i]]);if(x)return x;}
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var lk=String(k).toLowerCase();
            if(((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.

     if(u)return u;
        }
        return "";
    }

    var hit=direct(roleObj);
    if(hit)return hit;
    hit=direct(containerObj);
    if(hit)return hit;

    var best="",bestScore=-1,seen=[];
    function walk(node,depth){
        if(!node||depth>5||bestScore>=120)return;
        if(typeof node!=="object")return;
        if(seen.indexOf(node)>=0)return;
        seen.push(node);

        if(Array.isArray(node)){
            for(var ai=0;ai<node.length&&ai<24;ai++)walk(node[ai],depth+1);
            return;
        }

        for(var k in node){
            if(!Object.prototype.hasOwnProperty.call(node,k))continue;
            var v=node[k];

            if(typeof v==="string"){
                var u=qfRoleNormImg2940(v);
                if(!u)continue;

                var key=String(k||"").toLowerCase();
                var score=0;
                if(/role.*back.*ground/.test(key))score+=120;
                else if(/background/.test(key))score+=90;
                else if(/role.*image|role.*pic/.test(key))score+=75;
                else if(/portrait|photo/.test(key))score+=55;
                else if(/image|img|pic/.test(key))score+=40;
                else if(/avatar|head/.test(key))score+=20;

                if(/thumb|small|icon/.test(key))score-=35;
                if(/bookcover|cover/.test(u.toLowerCase()))score-=15;

                if(score>bestScore){
                    bestScore=score;
                    best=u;
                }
            }else if(v&&typeof v==="object"){
                walk(v,depth+1);
            }
        }
    }

    walk(roleObj,0);
    walk(containerObj,0);
    return best;
}

function qfRoleTags2940(roleObj,containerObj){
    var cand=[
        roleObj&&roleObj.TagList,roleObj&&roleObj.tagList,
        roleObj&&roleObj.Tags,roleObj&&roleObj.tags,
        roleObj&&roleObj.RoleTagList,roleObj&&roleObj.roleTagList,
        roleObj&&roleObj.CharacterTagList,roleObj&&roleObj.characterTagList,
        roleObj&&roleObj.HonorList,roleObj&&roleObj.honorList,
        roleObj&&roleObj.LabelList,roleObj&&roleObj.labelList,
        containerObj&&containerObj.TagList,containerObj&&containerObj.tagList,
        containerObj&&containerObj.RoleTagList,containerObj&&containerObj.roleTagList,
        containerObj&&containerObj.CharacterTagList,containerObj&&containerObj.characterTagList,
        containerObj&&containerObj.HonorList,containerObj&&containerObj.honorList,
        containerObj&&containerObj.LabelList,containerObj&&containerObj.labelList
    ];
    var out=[],seen={};
    for(var c=0;c<cand.length;c++){
        var a=qfRoleArr2940(cand[c]);
        for(var i=0;i<a.length&&out.length<24;i++){
            var t=a[i],name="",likes=0;
            if(typeof t==="string"){
                name=qfRolePlainB61(t);
            }else{
                t=t||{};
                var ti=t.TagInfo||t.tagInfo||t.LabelInfo||t.labelInfo||{};
                name=qfRolePlainB61(
                    t.TagName||t.tagName||t.Name||t.name||t.Title||t.title||
                    t.LabelName||t.labelName||ti.TagName||ti.tagName||ti.Name||ti.name||""
                );
                likes=qfRoleNum2943(
                    t.Likes||t.likes||t.LikeCount||t.likeCount||t.Count||t.count||
                    t.HotValue||t.hotValue||ti.Likes||ti.likes||ti.Count||ti.count||0
                );
            }
            if(!name||name.length>40||seen[name])continue;
            seen[name]=1;
            out.push({name:name,likes:likes||0});
        }
    }
    return out;
}

function qfRoleNum2943(v){
    if(v===undefined||v===null||v==="")return 0;
    if(typeof v==="number")return isFinite(v)?v:0;
    var s=String(v).replace(/,/g,"").trim();
    var m=s.match(/-?\d+(?:\.\d+)?/);
    return m?Number(m[0])||0:0;
}

function qfRolePrettyNum2943(n){
    n=Number(n||0);
    if(!isFinite(n)||n<=0)return "";
    if(n>=100000000){
        var y=n/100000000;
        return (Math.round(y*10)/10).toString().replace(/\.0$/,"")+"亿";
    }
    if(n>=10000){
        var w=n/10000;
        return (Math.round(w*10)/10).toString().replace(/\.0$/,"")+"万";
    }
    return String(Math.round(n));
}

function qfRoleNormalize2940(root,bid){
    var d=(root&&(root.Data||root.data))||root||{};
    var out=[],byId={},byName={};
    bid=String(bid||"");

    function cleanId(v){
        if(v===undefined||v===null)return "";
        var s=String(v).trim();
        if(!/^\d{5,}$/.test(s))return "";
        if(bid&&s===bid)return "";
        return s;
    }
    function directRoleId(o,allowGeneric){
        if(!o||typeof o!=="object")return "";
        var ks=[
            "RoleId","roleId","RoleID","roleID",
            "BookRoleId","bookRoleId","BookRoleID","bookRoleID",
            "CharacterId","characterId","CharacterID","characterID"
        ];
        for(var i=0;i<ks.length;i++){var x=cleanId(o[ks[i]]);if(x)return x;}
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var lk=String(k).toLowerCase();
            if(((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid

)return u;
        }
        return "";
    }

    var hit=direct(roleObj);
    if(hit)return hit;
    hit=direct(containerObj);
    if(hit)return hit;

    var best="",bestScore=-1,seen=[];
    function walk(node,depth){
        if(!node||depth>5||bestScore>=120)return;
        if(typeof node!=="object")return;
        if(seen.indexOf(node)>=0)return;
        seen.push(node);

        if(Array.isArray(node)){
            for(var ai=0;ai<node.length&&ai<24;ai++)walk(node[ai],depth+1);
            return;
        }

        for(var k in node){
            if(!Object.prototype.hasOwnProperty.call(node,k))continue;
            var v=node[k];

            if(typeof v==="string"){
                var u=qfRoleNormImg2940(v);
                if(!u)continue;

                var key=String(k||"").toLowerCase();
                var score=0;
                if(/role.*back.*ground/.test(key))score+=120;
                else if(/background/.test(key))score+=90;
                else if(/role.*image|role.*pic/.test(key))score+=75;
                else if(/portrait|photo/.test(key))score+=55;
                else if(/image|img|pic/.test(key))score+=40;
                else if(/avatar|head/.test(key))score+=20;

                if(/thumb|small|icon/.test(key))score-=35;
                if(/bookcover|cover/.test(u.toLowerCase()))score-=15;

                if(score>bestScore){
                    bestScore=score;
                    best=u;
                }
            }else if(v&&typeof v==="object"){
                walk(v,depth+1);
            }
        }
    }

    walk(roleObj,0);
    walk(containerObj,0);
    return best;
}

function qfRoleTags2940(roleObj,containerObj){
    var cand=[
        roleObj&&roleObj.TagList,roleObj&&roleObj.tagList,
        roleObj&&roleObj.Tags,roleObj&&roleObj.tags,
        roleObj&&roleObj.RoleTagList,roleObj&&roleObj.roleTagList,
        roleObj&&roleObj.CharacterTagList,roleObj&&roleObj.characterTagList,
        roleObj&&roleObj.HonorList,roleObj&&roleObj.honorList,
        roleObj&&roleObj.LabelList,roleObj&&roleObj.labelList,
        containerObj&&containerObj.TagList,containerObj&&containerObj.tagList,
        containerObj&&containerObj.RoleTagList,containerObj&&containerObj.roleTagList,
        containerObj&&containerObj.CharacterTagList,containerObj&&containerObj.characterTagList,
        containerObj&&containerObj.HonorList,containerObj&&containerObj.honorList,
        containerObj&&containerObj.LabelList,containerObj&&containerObj.labelList
    ];
    var out=[],seen={};
    for(var c=0;c<cand.length;c++){
        var a=qfRoleArr2940(cand[c]);
        for(var i=0;i<a.length&&out.length<24;i++){
            var t=a[i],name="",likes=0;
            if(typeof t==="string"){
                name=qfRolePlainB61(t);
            }else{
                t=t||{};
                var ti=t.TagInfo||t.tagInfo||t.LabelInfo||t.labelInfo||{};
                name=qfRolePlainB61(
                    t.TagName||t.tagName||t.Name||t.name||t.Title||t.title||
                    t.LabelName||t.labelName||ti.TagName||ti.tagName||ti.Name||ti.name||""
                );
                likes=qfRoleNum2943(
                    t.Likes||t.likes||t.LikeCount||t.likeCount||t.Count||t.count||
                    t.HotValue||t.hotValue||ti.Likes||ti.likes||ti.Count||ti.count||0
                );
            }
            if(!name||name.length>40||seen[name])continue;
            seen[name]=1;
            out.push({name:name,likes:likes||0});
        }
    }
    return out;
}

function qfRoleNum2943(v){
    if(v===undefined||v===null||v==="")return 0;
    if(typeof v==="number")return isFinite(v)?v:0;
    var s=String(v).replace(/,/g,"").trim();
    var m=s.match(/-?\d+(?:\.\d+)?/);
    return m?Number(m[0])||0:0;
}

function qfRolePrettyNum2943(n){
    n=Number(n||0);
    if(!isFinite(n)||n<=0)return "";
    if(n>=100000000){
        var y=n/100000000;
        return (Math.round(y*10)/10).toString().replace(/\.0$/,"")+"亿";
    }
    if(n>=10000){
        var w=n/10000;
        return (Math.round(w*10)/10).toString().replace(/\.0$/,"")+"万";
    }
    return String(Math.round(n));
}

function qfRoleNormalize2940(root,bid){
    var d=(root&&(root.Data||root.data))||root||{};
    var out=[],byId={},byName={};
    bid=String(bid||"");

    function cleanId(v){
        if(v===undefined||v===null)return "";
        var s=String(v).trim();
        if(!/^\d{5,}$/.test(s))return "";
        if(bid&&s===bid)return "";
        return s;
    }
    function directRoleId(o,allowGeneric){
        if(!o||typeof o!=="object")return "";
        var ks=[
            "RoleId","roleId","RoleID","roleID",
            "BookRoleId","bookRoleId","BookRoleID","bookRoleID",
            "CharacterId","characterId","CharacterID","characterID"
        ];
        for(var i=0;i<ks.length;i++){var x=cleanId(o[ks[i]]);if(x)return x;}
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var lk=String(k).toLowerCase();
            if(((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdBy

;
        }
        return "";
    }

    var hit=direct(roleObj);
    if(hit)return hit;
    hit=direct(containerObj);
    if(hit)return hit;

    var best="",bestScore=-1,seen=[];
    function walk(node,depth){
        if(!node||depth>5||bestScore>=120)return;
        if(typeof node!=="object")return;
        if(seen.indexOf(node)>=0)return;
        seen.push(node);

        if(Array.isArray(node)){
            for(var ai=0;ai<node.length&&ai<24;ai++)walk(node[ai],depth+1);
            return;
        }

        for(var k in node){
            if(!Object.prototype.hasOwnProperty.call(node,k))continue;
            var v=node[k];

            if(typeof v==="string"){
                var u=qfRoleNormImg2940(v);
                if(!u)continue;

                var key=String(k||"").toLowerCase();
                var score=0;
                if(/role.*back.*ground/.test(key))score+=120;
                else if(/background/.test(key))score+=90;
                else if(/role.*image|role.*pic/.test(key))score+=75;
                else if(/portrait|photo/.test(key))score+=55;
                else if(/image|img|pic/.test(key))score+=40;
                else if(/avatar|head/.test(key))score+=20;

                if(/thumb|small|icon/.test(key))score-=35;
                if(/bookcover|cover/.test(u.toLowerCase()))score-=15;

                if(score>bestScore){
                    bestScore=score;
                    best=u;
                }
            }else if(v&&typeof v==="object"){
                walk(v,depth+1);
            }
        }
    }

    walk(roleObj,0);
    walk(containerObj,0);
    return best;
}

function qfRoleTags2940(roleObj,containerObj){
    var cand=[
        roleObj&&roleObj.TagList,roleObj&&roleObj.tagList,
        roleObj&&roleObj.Tags,roleObj&&roleObj.tags,
        roleObj&&roleObj.RoleTagList,roleObj&&roleObj.roleTagList,
        roleObj&&roleObj.CharacterTagList,roleObj&&roleObj.characterTagList,
        roleObj&&roleObj.HonorList,roleObj&&roleObj.honorList,
        roleObj&&roleObj.LabelList,roleObj&&roleObj.labelList,
        containerObj&&containerObj.TagList,containerObj&&containerObj.tagList,
        containerObj&&containerObj.RoleTagList,containerObj&&containerObj.roleTagList,
        containerObj&&containerObj.CharacterTagList,containerObj&&containerObj.characterTagList,
        containerObj&&containerObj.HonorList,containerObj&&containerObj.honorList,
        containerObj&&containerObj.LabelList,containerObj&&containerObj.labelList
    ];
    var out=[],seen={};
    for(var c=0;c<cand.length;c++){
        var a=qfRoleArr2940(cand[c]);
        for(var i=0;i<a.length&&out.length<24;i++){
            var t=a[i],name="",likes=0;
            if(typeof t==="string"){
                name=qfRolePlainB61(t);
            }else{
                t=t||{};
                var ti=t.TagInfo||t.tagInfo||t.LabelInfo||t.labelInfo||{};
                name=qfRolePlainB61(
                    t.TagName||t.tagName||t.Name||t.name||t.Title||t.title||
                    t.LabelName||t.labelName||ti.TagName||ti.tagName||ti.Name||ti.name||""
                );
                likes=qfRoleNum2943(
                    t.Likes||t.likes||t.LikeCount||t.likeCount||t.Count||t.count||
                    t.HotValue||t.hotValue||ti.Likes||ti.likes||ti.Count||ti.count||0
                );
            }
            if(!name||name.length>40||seen[name])continue;
            seen[name]=1;
            out.push({name:name,likes:likes||0});
        }
    }
    return out;
}

function qfRoleNum2943(v){
    if(v===undefined||v===null||v==="")return 0;
    if(typeof v==="number")return isFinite(v)?v:0;
    var s=String(v).replace(/,/g,"").trim();
    var m=s.match(/-?\d+(?:\.\d+)?/);
    return m?Number(m[0])||0:0;
}

function qfRolePrettyNum2943(n){
    n=Number(n||0);
    if(!isFinite(n)||n<=0)return "";
    if(n>=100000000){
        var y=n/100000000;
        return (Math.round(y*10)/10).toString().replace(/\.0$/,"")+"亿";
    }
    if(n>=10000){
        var w=n/10000;
        return (Math.round(w*10)/10).toString().replace(/\.0$/,"")+"万";
    }
    return String(Math.round(n));
}

function qfRoleNormalize2940(root,bid){
    var d=(root&&(root.Data||root.data))||root||{};
    var out=[],byId={},byName={};
    bid=String(bid||"");

    function cleanId(v){
        if(v===undefined||v===null)return "";
        var s=String(v).trim();
        if(!/^\d{5,}$/.test(s))return "";
        if(bid&&s===bid)return "";
        return s;
    }
    function directRoleId(o,allowGeneric){
        if(!o||typeof o!=="object")return "";
        var ks=[
            "RoleId","roleId","RoleID","roleID",
            "BookRoleId","bookRoleId","BookRoleID","bookRoleID",
            "CharacterId","characterId","CharacterID","characterID"
        ];
        for(var i=0;i<ks.length;i++){var x=cleanId(o[ks[i]]);if(x)return x;}
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var lk=String(k).toLowerCase();
            if(((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[

       return "";
    }

    var hit=direct(roleObj);
    if(hit)return hit;
    hit=direct(containerObj);
    if(hit)return hit;

    var best="",bestScore=-1,seen=[];
    function walk(node,depth){
        if(!node||depth>5||bestScore>=120)return;
        if(typeof node!=="object")return;
        if(seen.indexOf(node)>=0)return;
        seen.push(node);

        if(Array.isArray(node)){
            for(var ai=0;ai<node.length&&ai<24;ai++)walk(node[ai],depth+1);
            return;
        }

        for(var k in node){
            if(!Object.prototype.hasOwnProperty.call(node,k))continue;
            var v=node[k];

            if(typeof v==="string"){
                var u=qfRoleNormImg2940(v);
                if(!u)continue;

                var key=String(k||"").toLowerCase();
                var score=0;
                if(/role.*back.*ground/.test(key))score+=120;
                else if(/background/.test(key))score+=90;
                else if(/role.*image|role.*pic/.test(key))score+=75;
                else if(/portrait|photo/.test(key))score+=55;
                else if(/image|img|pic/.test(key))score+=40;
                else if(/avatar|head/.test(key))score+=20;

                if(/thumb|small|icon/.test(key))score-=35;
                if(/bookcover|cover/.test(u.toLowerCase()))score-=15;

                if(score>bestScore){
                    bestScore=score;
                    best=u;
                }
            }else if(v&&typeof v==="object"){
                walk(v,depth+1);
            }
        }
    }

    walk(roleObj,0);
    walk(containerObj,0);
    return best;
}

function qfRoleTags2940(roleObj,containerObj){
    var cand=[
        roleObj&&roleObj.TagList,roleObj&&roleObj.tagList,
        roleObj&&roleObj.Tags,roleObj&&roleObj.tags,
        roleObj&&roleObj.RoleTagList,roleObj&&roleObj.roleTagList,
        roleObj&&roleObj.CharacterTagList,roleObj&&roleObj.characterTagList,
        roleObj&&roleObj.HonorList,roleObj&&roleObj.honorList,
        roleObj&&roleObj.LabelList,roleObj&&roleObj.labelList,
        containerObj&&containerObj.TagList,containerObj&&containerObj.tagList,
        containerObj&&containerObj.RoleTagList,containerObj&&containerObj.roleTagList,
        containerObj&&containerObj.CharacterTagList,containerObj&&containerObj.characterTagList,
        containerObj&&containerObj.HonorList,containerObj&&containerObj.honorList,
        containerObj&&containerObj.LabelList,containerObj&&containerObj.labelList
    ];
    var out=[],seen={};
    for(var c=0;c<cand.length;c++){
        var a=qfRoleArr2940(cand[c]);
        for(var i=0;i<a.length&&out.length<24;i++){
            var t=a[i],name="",likes=0;
            if(typeof t==="string"){
                name=qfRolePlainB61(t);
            }else{
                t=t||{};
                var ti=t.TagInfo||t.tagInfo||t.LabelInfo||t.labelInfo||{};
                name=qfRolePlainB61(
                    t.TagName||t.tagName||t.Name||t.name||t.Title||t.title||
                    t.LabelName||t.labelName||ti.TagName||ti.tagName||ti.Name||ti.name||""
                );
                likes=qfRoleNum2943(
                    t.Likes||t.likes||t.LikeCount||t.likeCount||t.Count||t.count||
                    t.HotValue||t.hotValue||ti.Likes||ti.likes||ti.Count||ti.count||0
                );
            }
            if(!name||name.length>40||seen[name])continue;
            seen[name]=1;
            out.push({name:name,likes:likes||0});
        }
    }
    return out;
}

function qfRoleNum2943(v){
    if(v===undefined||v===null||v==="")return 0;
    if(typeof v==="number")return isFinite(v)?v:0;
    var s=String(v).replace(/,/g,"").trim();
    var m=s.match(/-?\d+(?:\.\d+)?/);
    return m?Number(m[0])||0:0;
}

function qfRolePrettyNum2943(n){
    n=Number(n||0);
    if(!isFinite(n)||n<=0)return "";
    if(n>=100000000){
        var y=n/100000000;
        return (Math.round(y*10)/10).toString().replace(/\.0$/,"")+"亿";
    }
    if(n>=10000){
        var w=n/10000;
        return (Math.round(w*10)/10).toString().replace(/\.0$/,"")+"万";
    }
    return String(Math.round(n));
}

function qfRoleNormalize2940(root,bid){
    var d=(root&&(root.Data||root.data))||root||{};
    var out=[],byId={},byName={};
    bid=String(bid||"");

    function cleanId(v){
        if(v===undefined||v===null)return "";
        var s=String(v).trim();
        if(!/^\d{5,}$/.test(s))return "";
        if(bid&&s===bid)return "";
        return s;
    }
    function directRoleId(o,allowGeneric){
        if(!o||typeof o!=="object")return "";
        var ks=[
            "RoleId","roleId","RoleID","roleID",
            "BookRoleId","bookRoleId","BookRoleID","bookRoleID",
            "CharacterId","characterId","CharacterID","characterID"
        ];
        for(var i=0;i<ks.length;i++){var x=cleanId(o[ks[i]]);if(x)return x;}
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var lk=String(k).toLowerCase();
            if(((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[z].name);if(f

    hit=direct(containerObj);
    if(hit)return hit;

    var best="",bestScore=-1,seen=[];
    function walk(node,depth){
        if(!node||depth>5||bestScore>=120)return;
        if(typeof node!=="object")return;
        if(seen.indexOf(node)>=0)return;
        seen.push(node);

        if(Array.isArray(node)){
            for(var ai=0;ai<node.length&&ai<24;ai++)walk(node[ai],depth+1);
            return;
        }

        for(var k in node){
            if(!Object.prototype.hasOwnProperty.call(node,k))continue;
            var v=node[k];

            if(typeof v==="string"){
                var u=qfRoleNormImg2940(v);
                if(!u)continue;

                var key=String(k||"").toLowerCase();
                var score=0;
                if(/role.*back.*ground/.test(key))score+=120;
                else if(/background/.test(key))score+=90;
                else if(/role.*image|role.*pic/.test(key))score+=75;
                else if(/portrait|photo/.test(key))score+=55;
                else if(/image|img|pic/.test(key))score+=40;
                else if(/avatar|head/.test(key))score+=20;

                if(/thumb|small|icon/.test(key))score-=35;
                if(/bookcover|cover/.test(u.toLowerCase()))score-=15;

                if(score>bestScore){
                    bestScore=score;
                    best=u;
                }
            }else if(v&&typeof v==="object"){
                walk(v,depth+1);
            }
        }
    }

    walk(roleObj,0);
    walk(containerObj,0);
    return best;
}

function qfRoleTags2940(roleObj,containerObj){
    var cand=[
        roleObj&&roleObj.TagList,roleObj&&roleObj.tagList,
        roleObj&&roleObj.Tags,roleObj&&roleObj.tags,
        roleObj&&roleObj.RoleTagList,roleObj&&roleObj.roleTagList,
        roleObj&&roleObj.CharacterTagList,roleObj&&roleObj.characterTagList,
        roleObj&&roleObj.HonorList,roleObj&&roleObj.honorList,
        roleObj&&roleObj.LabelList,roleObj&&roleObj.labelList,
        containerObj&&containerObj.TagList,containerObj&&containerObj.tagList,
        containerObj&&containerObj.RoleTagList,containerObj&&containerObj.roleTagList,
        containerObj&&containerObj.CharacterTagList,containerObj&&containerObj.characterTagList,
        containerObj&&containerObj.HonorList,containerObj&&containerObj.honorList,
        containerObj&&containerObj.LabelList,containerObj&&containerObj.labelList
    ];
    var out=[],seen={};
    for(var c=0;c<cand.length;c++){
        var a=qfRoleArr2940(cand[c]);
        for(var i=0;i<a.length&&out.length<24;i++){
            var t=a[i],name="",likes=0;
            if(typeof t==="string"){
                name=qfRolePlainB61(t);
            }else{
                t=t||{};
                var ti=t.TagInfo||t.tagInfo||t.LabelInfo||t.labelInfo||{};
                name=qfRolePlainB61(
                    t.TagName||t.tagName||t.Name||t.name||t.Title||t.title||
                    t.LabelName||t.labelName||ti.TagName||ti.tagName||ti.Name||ti.name||""
                );
                likes=qfRoleNum2943(
                    t.Likes||t.likes||t.LikeCount||t.likeCount||t.Count||t.count||
                    t.HotValue||t.hotValue||ti.Likes||ti.likes||ti.Count||ti.count||0
                );
            }
            if(!name||name.length>40||seen[name])continue;
            seen[name]=1;
            out.push({name:name,likes:likes||0});
        }
    }
    return out;
}

function qfRoleNum2943(v){
    if(v===undefined||v===null||v==="")return 0;
    if(typeof v==="number")return isFinite(v)?v:0;
    var s=String(v).replace(/,/g,"").trim();
    var m=s.match(/-?\d+(?:\.\d+)?/);
    return m?Number(m[0])||0:0;
}

function qfRolePrettyNum2943(n){
    n=Number(n||0);
    if(!isFinite(n)||n<=0)return "";
    if(n>=100000000){
        var y=n/100000000;
        return (Math.round(y*10)/10).toString().replace(/\.0$/,"")+"亿";
    }
    if(n>=10000){
        var w=n/10000;
        return (Math.round(w*10)/10).toString().replace(/\.0$/,"")+"万";
    }
    return String(Math.round(n));
}

function qfRoleNormalize2940(root,bid){
    var d=(root&&(root.Data||root.data))||root||{};
    var out=[],byId={},byName={};
    bid=String(bid||"");

    function cleanId(v){
        if(v===undefined||v===null)return "";
        var s=String(v).trim();
        if(!/^\d{5,}$/.test(s))return "";
        if(bid&&s===bid)return "";
        return s;
    }
    function directRoleId(o,allowGeneric){
        if(!o||typeof o!=="object")return "";
        var ks=[
            "RoleId","roleId","RoleID","roleID",
            "BookRoleId","bookRoleId","BookRoleID","bookRoleID",
            "CharacterId","characterId","CharacterID","characterID"
        ];
        for(var i=0;i<ks.length;i++){var x=cleanId(o[ks[i]]);if(x)return x;}
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var lk=String(k).toLowerCase();
            if(((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[z].name);if(fid){out[z].id=fid;byId[fid]=z;}}
    }

    var cover="https://bookcover.yuew

rect(containerObj);
    if(hit)return hit;

    var best="",bestScore=-1,seen=[];
    function walk(node,depth){
        if(!node||depth>5||bestScore>=120)return;
        if(typeof node!=="object")return;
        if(seen.indexOf(node)>=0)return;
        seen.push(node);

        if(Array.isArray(node)){
            for(var ai=0;ai<node.length&&ai<24;ai++)walk(node[ai],depth+1);
            return;
        }

        for(var k in node){
            if(!Object.prototype.hasOwnProperty.call(node,k))continue;
            var v=node[k];

            if(typeof v==="string"){
                var u=qfRoleNormImg2940(v);
                if(!u)continue;

                var key=String(k||"").toLowerCase();
                var score=0;
                if(/role.*back.*ground/.test(key))score+=120;
                else if(/background/.test(key))score+=90;
                else if(/role.*image|role.*pic/.test(key))score+=75;
                else if(/portrait|photo/.test(key))score+=55;
                else if(/image|img|pic/.test(key))score+=40;
                else if(/avatar|head/.test(key))score+=20;

                if(/thumb|small|icon/.test(key))score-=35;
                if(/bookcover|cover/.test(u.toLowerCase()))score-=15;

                if(score>bestScore){
                    bestScore=score;
                    best=u;
                }
            }else if(v&&typeof v==="object"){
                walk(v,depth+1);
            }
        }
    }

    walk(roleObj,0);
    walk(containerObj,0);
    return best;
}

function qfRoleTags2940(roleObj,containerObj){
    var cand=[
        roleObj&&roleObj.TagList,roleObj&&roleObj.tagList,
        roleObj&&roleObj.Tags,roleObj&&roleObj.tags,
        roleObj&&roleObj.RoleTagList,roleObj&&roleObj.roleTagList,
        roleObj&&roleObj.CharacterTagList,roleObj&&roleObj.characterTagList,
        roleObj&&roleObj.HonorList,roleObj&&roleObj.honorList,
        roleObj&&roleObj.LabelList,roleObj&&roleObj.labelList,
        containerObj&&containerObj.TagList,containerObj&&containerObj.tagList,
        containerObj&&containerObj.RoleTagList,containerObj&&containerObj.roleTagList,
        containerObj&&containerObj.CharacterTagList,containerObj&&containerObj.characterTagList,
        containerObj&&containerObj.HonorList,containerObj&&containerObj.honorList,
        containerObj&&containerObj.LabelList,containerObj&&containerObj.labelList
    ];
    var out=[],seen={};
    for(var c=0;c<cand.length;c++){
        var a=qfRoleArr2940(cand[c]);
        for(var i=0;i<a.length&&out.length<24;i++){
            var t=a[i],name="",likes=0;
            if(typeof t==="string"){
                name=qfRolePlainB61(t);
            }else{
                t=t||{};
                var ti=t.TagInfo||t.tagInfo||t.LabelInfo||t.labelInfo||{};
                name=qfRolePlainB61(
                    t.TagName||t.tagName||t.Name||t.name||t.Title||t.title||
                    t.LabelName||t.labelName||ti.TagName||ti.tagName||ti.Name||ti.name||""
                );
                likes=qfRoleNum2943(
                    t.Likes||t.likes||t.LikeCount||t.likeCount||t.Count||t.count||
                    t.HotValue||t.hotValue||ti.Likes||ti.likes||ti.Count||ti.count||0
                );
            }
            if(!name||name.length>40||seen[name])continue;
            seen[name]=1;
            out.push({name:name,likes:likes||0});
        }
    }
    return out;
}

function qfRoleNum2943(v){
    if(v===undefined||v===null||v==="")return 0;
    if(typeof v==="number")return isFinite(v)?v:0;
    var s=String(v).replace(/,/g,"").trim();
    var m=s.match(/-?\d+(?:\.\d+)?/);
    return m?Number(m[0])||0:0;
}

function qfRolePrettyNum2943(n){
    n=Number(n||0);
    if(!isFinite(n)||n<=0)return "";
    if(n>=100000000){
        var y=n/100000000;
        return (Math.round(y*10)/10).toString().replace(/\.0$/,"")+"亿";
    }
    if(n>=10000){
        var w=n/10000;
        return (Math.round(w*10)/10).toString().replace(/\.0$/,"")+"万";
    }
    return String(Math.round(n));
}

function qfRoleNormalize2940(root,bid){
    var d=(root&&(root.Data||root.data))||root||{};
    var out=[],byId={},byName={};
    bid=String(bid||"");

    function cleanId(v){
        if(v===undefined||v===null)return "";
        var s=String(v).trim();
        if(!/^\d{5,}$/.test(s))return "";
        if(bid&&s===bid)return "";
        return s;
    }
    function directRoleId(o,allowGeneric){
        if(!o||typeof o!=="object")return "";
        var ks=[
            "RoleId","roleId","RoleID","roleID",
            "BookRoleId","bookRoleId","BookRoleID","bookRoleID",
            "CharacterId","characterId","CharacterID","characterID"
        ];
        for(var i=0;i<ks.length;i++){var x=cleanId(o[ks[i]]);if(x)return x;}
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var lk=String(k).toLowerCase();
            if(((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[z].name);if(fid){out[z].id=fid;byId[fid]=z;}}
    }

    var cover="https://bookcover.yuewen.com/qdb

f node!=="object")return;
        if(seen.indexOf(node)>=0)return;
        seen.push(node);

        if(Array.isArray(node)){
            for(var ai=0;ai<node.length&&ai<24;ai++)walk(node[ai],depth+1);
            return;
        }

        for(var k in node){
            if(!Object.prototype.hasOwnProperty.call(node,k))continue;
            var v=node[k];

            if(typeof v==="string"){
                var u=qfRoleNormImg2940(v);
                if(!u)continue;

                var key=String(k||"").toLowerCase();
                var score=0;
                if(/role.*back.*ground/.test(key))score+=120;
                else if(/background/.test(key))score+=90;
                else if(/role.*image|role.*pic/.test(key))score+=75;
                else if(/portrait|photo/.test(key))score+=55;
                else if(/image|img|pic/.test(key))score+=40;
                else if(/avatar|head/.test(key))score+=20;

                if(/thumb|small|icon/.test(key))score-=35;
                if(/bookcover|cover/.test(u.toLowerCase()))score-=15;

                if(score>bestScore){
                    bestScore=score;
                    best=u;
                }
            }else if(v&&typeof v==="object"){
                walk(v,depth+1);
            }
        }
    }

    walk(roleObj,0);
    walk(containerObj,0);
    return best;
}

function qfRoleTags2940(roleObj,containerObj){
    var cand=[
        roleObj&&roleObj.TagList,roleObj&&roleObj.tagList,
        roleObj&&roleObj.Tags,roleObj&&roleObj.tags,
        roleObj&&roleObj.RoleTagList,roleObj&&roleObj.roleTagList,
        roleObj&&roleObj.CharacterTagList,roleObj&&roleObj.characterTagList,
        roleObj&&roleObj.HonorList,roleObj&&roleObj.honorList,
        roleObj&&roleObj.LabelList,roleObj&&roleObj.labelList,
        containerObj&&containerObj.TagList,containerObj&&containerObj.tagList,
        containerObj&&containerObj.RoleTagList,containerObj&&containerObj.roleTagList,
        containerObj&&containerObj.CharacterTagList,containerObj&&containerObj.characterTagList,
        containerObj&&containerObj.HonorList,containerObj&&containerObj.honorList,
        containerObj&&containerObj.LabelList,containerObj&&containerObj.labelList
    ];
    var out=[],seen={};
    for(var c=0;c<cand.length;c++){
        var a=qfRoleArr2940(cand[c]);
        for(var i=0;i<a.length&&out.length<24;i++){
            var t=a[i],name="",likes=0;
            if(typeof t==="string"){
                name=qfRolePlainB61(t);
            }else{
                t=t||{};
                var ti=t.TagInfo||t.tagInfo||t.LabelInfo||t.labelInfo||{};
                name=qfRolePlainB61(
                    t.TagName||t.tagName||t.Name||t.name||t.Title||t.title||
                    t.LabelName||t.labelName||ti.TagName||ti.tagName||ti.Name||ti.name||""
                );
                likes=qfRoleNum2943(
                    t.Likes||t.likes||t.LikeCount||t.likeCount||t.Count||t.count||
                    t.HotValue||t.hotValue||ti.Likes||ti.likes||ti.Count||ti.count||0
                );
            }
            if(!name||name.length>40||seen[name])continue;
            seen[name]=1;
            out.push({name:name,likes:likes||0});
        }
    }
    return out;
}

function qfRoleNum2943(v){
    if(v===undefined||v===null||v==="")return 0;
    if(typeof v==="number")return isFinite(v)?v:0;
    var s=String(v).replace(/,/g,"").trim();
    var m=s.match(/-?\d+(?:\.\d+)?/);
    return m?Number(m[0])||0:0;
}

function qfRolePrettyNum2943(n){
    n=Number(n||0);
    if(!isFinite(n)||n<=0)return "";
    if(n>=100000000){
        var y=n/100000000;
        return (Math.round(y*10)/10).toString().replace(/\.0$/,"")+"亿";
    }
    if(n>=10000){
        var w=n/10000;
        return (Math.round(w*10)/10).toString().replace(/\.0$/,"")+"万";
    }
    return String(Math.round(n));
}

function qfRoleNormalize2940(root,bid){
    var d=(root&&(root.Data||root.data))||root||{};
    var out=[],byId={},byName={};
    bid=String(bid||"");

    function cleanId(v){
        if(v===undefined||v===null)return "";
        var s=String(v).trim();
        if(!/^\d{5,}$/.test(s))return "";
        if(bid&&s===bid)return "";
        return s;
    }
    function directRoleId(o,allowGeneric){
        if(!o||typeof o!=="object")return "";
        var ks=[
            "RoleId","roleId","RoleID","roleID",
            "BookRoleId","bookRoleId","BookRoleID","bookRoleID",
            "CharacterId","characterId","CharacterID","characterID"
        ];
        for(var i=0;i<ks.length;i++){var x=cleanId(o[ks[i]]);if(x)return x;}
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var lk=String(k).toLowerCase();
            if(((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[z].name);if(fid){out[z].id=fid;byId[fid]=z;}}
    }

    var cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600";
    for(var q=0;q<out.length;q++)if(!out[q].image)out[q].image=cover;
    return out;
}

function qfRoleMetaB22(raw){
    var 

!=="object")return;
        if(seen.indexOf(node)>=0)return;
        seen.push(node);

        if(Array.isArray(node)){
            for(var ai=0;ai<node.length&&ai<24;ai++)walk(node[ai],depth+1);
            return;
        }

        for(var k in node){
            if(!Object.prototype.hasOwnProperty.call(node,k))continue;
            var v=node[k];

            if(typeof v==="string"){
                var u=qfRoleNormImg2940(v);
                if(!u)continue;

                var key=String(k||"").toLowerCase();
                var score=0;
                if(/role.*back.*ground/.test(key))score+=120;
                else if(/background/.test(key))score+=90;
                else if(/role.*image|role.*pic/.test(key))score+=75;
                else if(/portrait|photo/.test(key))score+=55;
                else if(/image|img|pic/.test(key))score+=40;
                else if(/avatar|head/.test(key))score+=20;

                if(/thumb|small|icon/.test(key))score-=35;
                if(/bookcover|cover/.test(u.toLowerCase()))score-=15;

                if(score>bestScore){
                    bestScore=score;
                    best=u;
                }
            }else if(v&&typeof v==="object"){
                walk(v,depth+1);
            }
        }
    }

    walk(roleObj,0);
    walk(containerObj,0);
    return best;
}

function qfRoleTags2940(roleObj,containerObj){
    var cand=[
        roleObj&&roleObj.TagList,roleObj&&roleObj.tagList,
        roleObj&&roleObj.Tags,roleObj&&roleObj.tags,
        roleObj&&roleObj.RoleTagList,roleObj&&roleObj.roleTagList,
        roleObj&&roleObj.CharacterTagList,roleObj&&roleObj.characterTagList,
        roleObj&&roleObj.HonorList,roleObj&&roleObj.honorList,
        roleObj&&roleObj.LabelList,roleObj&&roleObj.labelList,
        containerObj&&containerObj.TagList,containerObj&&containerObj.tagList,
        containerObj&&containerObj.RoleTagList,containerObj&&containerObj.roleTagList,
        containerObj&&containerObj.CharacterTagList,containerObj&&containerObj.characterTagList,
        containerObj&&containerObj.HonorList,containerObj&&containerObj.honorList,
        containerObj&&containerObj.LabelList,containerObj&&containerObj.labelList
    ];
    var out=[],seen={};
    for(var c=0;c<cand.length;c++){
        var a=qfRoleArr2940(cand[c]);
        for(var i=0;i<a.length&&out.length<24;i++){
            var t=a[i],name="",likes=0;
            if(typeof t==="string"){
                name=qfRolePlainB61(t);
            }else{
                t=t||{};
                var ti=t.TagInfo||t.tagInfo||t.LabelInfo||t.labelInfo||{};
                name=qfRolePlainB61(
                    t.TagName||t.tagName||t.Name||t.name||t.Title||t.title||
                    t.LabelName||t.labelName||ti.TagName||ti.tagName||ti.Name||ti.name||""
                );
                likes=qfRoleNum2943(
                    t.Likes||t.likes||t.LikeCount||t.likeCount||t.Count||t.count||
                    t.HotValue||t.hotValue||ti.Likes||ti.likes||ti.Count||ti.count||0
                );
            }
            if(!name||name.length>40||seen[name])continue;
            seen[name]=1;
            out.push({name:name,likes:likes||0});
        }
    }
    return out;
}

function qfRoleNum2943(v){
    if(v===undefined||v===null||v==="")return 0;
    if(typeof v==="number")return isFinite(v)?v:0;
    var s=String(v).replace(/,/g,"").trim();
    var m=s.match(/-?\d+(?:\.\d+)?/);
    return m?Number(m[0])||0:0;
}

function qfRolePrettyNum2943(n){
    n=Number(n||0);
    if(!isFinite(n)||n<=0)return "";
    if(n>=100000000){
        var y=n/100000000;
        return (Math.round(y*10)/10).toString().replace(/\.0$/,"")+"亿";
    }
    if(n>=10000){
        var w=n/10000;
        return (Math.round(w*10)/10).toString().replace(/\.0$/,"")+"万";
    }
    return String(Math.round(n));
}

function qfRoleNormalize2940(root,bid){
    var d=(root&&(root.Data||root.data))||root||{};
    var out=[],byId={},byName={};
    bid=String(bid||"");

    function cleanId(v){
        if(v===undefined||v===null)return "";
        var s=String(v).trim();
        if(!/^\d{5,}$/.test(s))return "";
        if(bid&&s===bid)return "";
        return s;
    }
    function directRoleId(o,allowGeneric){
        if(!o||typeof o!=="object")return "";
        var ks=[
            "RoleId","roleId","RoleID","roleID",
            "BookRoleId","bookRoleId","BookRoleID","bookRoleID",
            "CharacterId","characterId","CharacterID","characterID"
        ];
        for(var i=0;i<ks.length;i++){var x=cleanId(o[ks[i]]);if(x)return x;}
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var lk=String(k).toLowerCase();
            if(((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[z].name);if(fid){out[z].id=fid;byId[fid]=z;}}
    }

    var cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600";
    for(var q=0;q<out.length;q++)if(!out[q].image)out[q].image=cover;
    return out;
}

function qfRoleMetaB22(raw){
    var profil

))continue;
            var lk=String(k).toLowerCase();
            if(((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[z].name);if(fid){out[z].id=fid;byId[fid]=z;}}
    }

    var cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600";
    for(var q=0;q<out.length;q++)if(!out[q].image)out[q].image=cover;
    return out;
}

function qfRoleMetaB22(raw){
    var profile={gender:"",birthday:"",constellation:"",age:"",identity:"",faction:""};
    var star={rank:"",value:"",level:"",guardians:"",current:0,next:0,percent:-1};
    if(!raw||typeof raw!=="object")return {profile:profile,star:star};
    var seen=[];
    function clean(v){
        if(v===undefined||v===null)return "";
        if(typeof v==="number"){if(!isFinite(v))return "";return String(v);}
        if(typeof v==="boolean")return v?"是":"否";
        if(typeof v!=="string")return "";
        var s=qfRolePlainB61(v);return s&&s!=="0"?s:"";
    }
    function primitive(o,names){
        if(!o||typeof o!=="object"||Array.isArray(o))return "";
        var map={};for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k))map[String(k).toLowerCase()]=k;
        for(var i=0;i<names.length;i++){
            var rk=map[String(names[i]).toLowerCase()];if(rk===undefined)continue;
            var x=o[rk];if(x!==null&&typeof x!=="object"){var s=clean(x);if(s)return s;}
        }
        return "";
    }
    function num(o,names){var s=primitive(o,names);if(!s)return 0;var n=Number(String(s).replace(/,/g,""));return isFinite(n)?n:0;}
    function walk(o,d){
        if(!o||typeof o!=="object"||d>5||seen.indexOf(o)>=0)return;seen.push(o);
        if(!profile.gender){var g=primitive(o,["GenderName","SexName","Gender","Sex"]);if(g&&!/^\d+$/.test(g))profile.gender=g;}
        if(!profile.birthday)profile.birthday=primitive(o,["Birthday","BirthDay","BirthDate","DateOfBirth"]);
        if(!profile.constellation)profile.constellation=primitive(o,["Constellation","StarSign","Zodiac"]);
        if(!profile.age){var a=primitive(o,["Age"]);if(a&&!/^0$/.test(a))profile.age=a;}
        if(!profile.identity)profile.identity=primitive(o,["IdentityName","RoleIdentity","Occupation","Career","Profession"]);
        if(!profile.faction)profile.faction=primitive(o,["FactionName","CampName","OrganizationName","ForceName"]);
        if(!star.rank)star.rank=primitive(o,["GuardianRank","GuardRank","RankNo","Rank","Ranking","SupportRank"]);
        if(!star.value)star.value=primitive(o,["StarValue","StarLightValue","GloryValue","SupportValue","RelationshipValue","RelationValue"]);
        if(!star.level)star.level=primitive(o,["StarLevelName","LevelName","RelationshipLevelName","RelationLevelName","GuardianLevelName"]);
        if(!star.guardians)star.guardians=primitive(o,["GuardianCount","GuardCount","SupporterCount","FansCount"]);
        if(!star.current)star.current=num(o,["CurrentValue","CurrentExp","CurrentScore","CurrentStarValue","CurValue"]);
        if(!star.next)star.next=num(o,["NextValue","NextExp","NextScore","NextStarValue","UpgradeValue","TargetValue"]);
        if(Array.isArray(o)){for(var i=0;i<o.length&&i<30;i++)walk(o[i],d+1);return;}
        for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k)){var x=o[k];if(x&&typeof x==="object")walk(x,d+1);}
    }
    walk(raw,0);
    if(star.current>0&&star.next>star.current)star.percent=Math.max(0,Math.min(100,Math.round(star.current*100/star.next)));
    return {profile:profile,star:star};
}

function qfRoleBookName2940(root,bookObj){
    try{
        var n=String(bookObj&&bookObj.name||"").trim();
        if(n)return n;
    }catch(e0){}

    var d=(root&&(root.Data||root.data))||root||{};
    var bi=d.BaseBookInfo||d.baseBookInfo||d.BookInfo||d.bookInfo||{};
    return String(
        bi.BookName||bi.bookName||
        bi.Name||bi.name||
        d.BookName||d.bookName||
        "书籍信息"
    ).trim()||"书籍信息";
}

function qfRoleHtml2940(root,bid,bookObj,roles){
    var bookName=qfRoleBookName2940(root,bookObj),cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600",cards="";
    function v(x){return qfRoleEsc2940(String(x==null?"":x));}
    function nice(x){var n=Number(x);return isFinite(n)&&n>0?qfRolePrettyNum2943(n):v(x);}
    for(var i=0;i<roles.length;i++){
        var r=roles[i]||{},meta=(r.profile||r.star)?{profile:r.profile||{},star:r.star||{}}:qfRoleMetaB22(r.raw||{}),p=meta.profile||{},s=meta.star||{},tags="",rows="";
        for(var k=0;k<(r.tags||[]).length;k++){var t=r.tags[k]||{};tags+='<span class="roleTag">'+v(t.name)+(t.likes>0?'<b> · '+v(qfRolePrettyNum2943(t.likes))+'</b>':'')+'</span>';}
        function row(label,val){if(val)rows+='<div class="kv"><span>'+label+'</span><b>'+v(val)+'</b></div>';}
        row('性别',p.gender);row('生日',p.birthday);row('星座',p.constellation);row('年龄',p.age);row('身份',p.identity);row('阵营',p.faction);
        var hasStar=!!(s.rank||s.value||s.level||s.guardians||s.current||s.next),prog=(Number(s.percent)>=0)?('<div class="progress"><i style="width:'+Math.max(0,Math.min(100,Number(s.percent)))

toLowerCase();
            if(((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[z].name);if(fid){out[z].id=fid;byId[fid]=z;}}
    }

    var cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600";
    for(var q=0;q<out.length;q++)if(!out[q].image)out[q].image=cover;
    return out;
}

function qfRoleMetaB22(raw){
    var profile={gender:"",birthday:"",constellation:"",age:"",identity:"",faction:""};
    var star={rank:"",value:"",level:"",guardians:"",current:0,next:0,percent:-1};
    if(!raw||typeof raw!=="object")return {profile:profile,star:star};
    var seen=[];
    function clean(v){
        if(v===undefined||v===null)return "";
        if(typeof v==="number"){if(!isFinite(v))return "";return String(v);}
        if(typeof v==="boolean")return v?"是":"否";
        if(typeof v!=="string")return "";
        var s=qfRolePlainB61(v);return s&&s!=="0"?s:"";
    }
    function primitive(o,names){
        if(!o||typeof o!=="object"||Array.isArray(o))return "";
        var map={};for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k))map[String(k).toLowerCase()]=k;
        for(var i=0;i<names.length;i++){
            var rk=map[String(names[i]).toLowerCase()];if(rk===undefined)continue;
            var x=o[rk];if(x!==null&&typeof x!=="object"){var s=clean(x);if(s)return s;}
        }
        return "";
    }
    function num(o,names){var s=primitive(o,names);if(!s)return 0;var n=Number(String(s).replace(/,/g,""));return isFinite(n)?n:0;}
    function walk(o,d){
        if(!o||typeof o!=="object"||d>5||seen.indexOf(o)>=0)return;seen.push(o);
        if(!profile.gender){var g=primitive(o,["GenderName","SexName","Gender","Sex"]);if(g&&!/^\d+$/.test(g))profile.gender=g;}
        if(!profile.birthday)profile.birthday=primitive(o,["Birthday","BirthDay","BirthDate","DateOfBirth"]);
        if(!profile.constellation)profile.constellation=primitive(o,["Constellation","StarSign","Zodiac"]);
        if(!profile.age){var a=primitive(o,["Age"]);if(a&&!/^0$/.test(a))profile.age=a;}
        if(!profile.identity)profile.identity=primitive(o,["IdentityName","RoleIdentity","Occupation","Career","Profession"]);
        if(!profile.faction)profile.faction=primitive(o,["FactionName","CampName","OrganizationName","ForceName"]);
        if(!star.rank)star.rank=primitive(o,["GuardianRank","GuardRank","RankNo","Rank","Ranking","SupportRank"]);
        if(!star.value)star.value=primitive(o,["StarValue","StarLightValue","GloryValue","SupportValue","RelationshipValue","RelationValue"]);
        if(!star.level)star.level=primitive(o,["StarLevelName","LevelName","RelationshipLevelName","RelationLevelName","GuardianLevelName"]);
        if(!star.guardians)star.guardians=primitive(o,["GuardianCount","GuardCount","SupporterCount","FansCount"]);
        if(!star.current)star.current=num(o,["CurrentValue","CurrentExp","CurrentScore","CurrentStarValue","CurValue"]);
        if(!star.next)star.next=num(o,["NextValue","NextExp","NextScore","NextStarValue","UpgradeValue","TargetValue"]);
        if(Array.isArray(o)){for(var i=0;i<o.length&&i<30;i++)walk(o[i],d+1);return;}
        for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k)){var x=o[k];if(x&&typeof x==="object")walk(x,d+1);}
    }
    walk(raw,0);
    if(star.current>0&&star.next>star.current)star.percent=Math.max(0,Math.min(100,Math.round(star.current*100/star.next)));
    return {profile:profile,star:star};
}

function qfRoleBookName2940(root,bookObj){
    try{
        var n=String(bookObj&&bookObj.name||"").trim();
        if(n)return n;
    }catch(e0){}

    var d=(root&&(root.Data||root.data))||root||{};
    var bi=d.BaseBookInfo||d.baseBookInfo||d.BookInfo||d.bookInfo||{};
    return String(
        bi.BookName||bi.bookName||
        bi.Name||bi.name||
        d.BookName||d.bookName||
        "书籍信息"
    ).trim()||"书籍信息";
}

function qfRoleHtml2940(root,bid,bookObj,roles){
    var bookName=qfRoleBookName2940(root,bookObj),cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600",cards="";
    function v(x){return qfRoleEsc2940(String(x==null?"":x));}
    function nice(x){var n=Number(x);return isFinite(n)&&n>0?qfRolePrettyNum2943(n):v(x);}
    for(var i=0;i<roles.length;i++){
        var r=roles[i]||{},meta=(r.profile||r.star)?{profile:r.profile||{},star:r.star||{}}:qfRoleMetaB22(r.raw||{}),p=meta.profile||{},s=meta.star||{},tags="",rows="";
        for(var k=0;k<(r.tags||[]).length;k++){var t=r.tags[k]||{};tags+='<span class="roleTag">'+v(t.name)+(t.likes>0?'<b> · '+v(qfRolePrettyNum2943(t.likes))+'</b>':'')+'</span>';}
        function row(label,val){if(val)rows+='<div class="kv"><span>'+label+'</span><b>'+v(val)+'</b></div>';}
        row('性别',p.gender);row('生日',p.birthday);row('星座',p.constellation);row('年龄',p.age);row('身份',p.identity);row('阵营',p.faction);
        var hasStar=!!(s.rank||s.value||s.level||s.guardians||s.current||s.next),prog=(Number(s.percent)>=0)?('<div class="progress"><i style="width:'+Math.max(0,Math.min(100,Number(s.percent)))+'%"></i></div><div class="progressText">

            if(((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[z].name);if(fid){out[z].id=fid;byId[fid]=z;}}
    }

    var cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600";
    for(var q=0;q<out.length;q++)if(!out[q].image)out[q].image=cover;
    return out;
}

function qfRoleMetaB22(raw){
    var profile={gender:"",birthday:"",constellation:"",age:"",identity:"",faction:""};
    var star={rank:"",value:"",level:"",guardians:"",current:0,next:0,percent:-1};
    if(!raw||typeof raw!=="object")return {profile:profile,star:star};
    var seen=[];
    function clean(v){
        if(v===undefined||v===null)return "";
        if(typeof v==="number"){if(!isFinite(v))return "";return String(v);}
        if(typeof v==="boolean")return v?"是":"否";
        if(typeof v!=="string")return "";
        var s=qfRolePlainB61(v);return s&&s!=="0"?s:"";
    }
    function primitive(o,names){
        if(!o||typeof o!=="object"||Array.isArray(o))return "";
        var map={};for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k))map[String(k).toLowerCase()]=k;
        for(var i=0;i<names.length;i++){
            var rk=map[String(names[i]).toLowerCase()];if(rk===undefined)continue;
            var x=o[rk];if(x!==null&&typeof x!=="object"){var s=clean(x);if(s)return s;}
        }
        return "";
    }
    function num(o,names){var s=primitive(o,names);if(!s)return 0;var n=Number(String(s).replace(/,/g,""));return isFinite(n)?n:0;}
    function walk(o,d){
        if(!o||typeof o!=="object"||d>5||seen.indexOf(o)>=0)return;seen.push(o);
        if(!profile.gender){var g=primitive(o,["GenderName","SexName","Gender","Sex"]);if(g&&!/^\d+$/.test(g))profile.gender=g;}
        if(!profile.birthday)profile.birthday=primitive(o,["Birthday","BirthDay","BirthDate","DateOfBirth"]);
        if(!profile.constellation)profile.constellation=primitive(o,["Constellation","StarSign","Zodiac"]);
        if(!profile.age){var a=primitive(o,["Age"]);if(a&&!/^0$/.test(a))profile.age=a;}
        if(!profile.identity)profile.identity=primitive(o,["IdentityName","RoleIdentity","Occupation","Career","Profession"]);
        if(!profile.faction)profile.faction=primitive(o,["FactionName","CampName","OrganizationName","ForceName"]);
        if(!star.rank)star.rank=primitive(o,["GuardianRank","GuardRank","RankNo","Rank","Ranking","SupportRank"]);
        if(!star.value)star.value=primitive(o,["StarValue","StarLightValue","GloryValue","SupportValue","RelationshipValue","RelationValue"]);
        if(!star.level)star.level=primitive(o,["StarLevelName","LevelName","RelationshipLevelName","RelationLevelName","GuardianLevelName"]);
        if(!star.guardians)star.guardians=primitive(o,["GuardianCount","GuardCount","SupporterCount","FansCount"]);
        if(!star.current)star.current=num(o,["CurrentValue","CurrentExp","CurrentScore","CurrentStarValue","CurValue"]);
        if(!star.next)star.next=num(o,["NextValue","NextExp","NextScore","NextStarValue","UpgradeValue","TargetValue"]);
        if(Array.isArray(o)){for(var i=0;i<o.length&&i<30;i++)walk(o[i],d+1);return;}
        for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k)){var x=o[k];if(x&&typeof x==="object")walk(x,d+1);}
    }
    walk(raw,0);
    if(star.current>0&&star.next>star.current)star.percent=Math.max(0,Math.min(100,Math.round(star.current*100/star.next)));
    return {profile:profile,star:star};
}

function qfRoleBookName2940(root,bookObj){
    try{
        var n=String(bookObj&&bookObj.name||"").trim();
        if(n)return n;
    }catch(e0){}

    var d=(root&&(root.Data||root.data))||root||{};
    var bi=d.BaseBookInfo||d.baseBookInfo||d.BookInfo||d.bookInfo||{};
    return String(
        bi.BookName||bi.bookName||
        bi.Name||bi.name||
        d.BookName||d.bookName||
        "书籍信息"
    ).trim()||"书籍信息";
}

function qfRoleHtml2940(root,bid,bookObj,roles){
    var bookName=qfRoleBookName2940(root,bookObj),cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600",cards="";
    function v(x){return qfRoleEsc2940(String(x==null?"":x));}
    function nice(x){var n=Number(x);return isFinite(n)&&n>0?qfRolePrettyNum2943(n):v(x);}
    for(var i=0;i<roles.length;i++){
        var r=roles[i]||{},meta=(r.profile||r.star)?{profile:r.profile||{},star:r.star||{}}:qfRoleMetaB22(r.raw||{}),p=meta.profile||{},s=meta.star||{},tags="",rows="";
        for(var k=0;k<(r.tags||[]).length;k++){var t=r.tags[k]||{};tags+='<span class="roleTag">'+v(t.name)+(t.likes>0?'<b> · '+v(qfRolePrettyNum2943(t.likes))+'</b>':'')+'</span>';}
        function row(label,val){if(val)rows+='<div class="kv"><span>'+label+'</span><b>'+v(val)+'</b></div>';}
        row('性别',p.gender);row('生日',p.birthday);row('星座',p.constellation);row('年龄',p.age);row('身份',p.identity);row('阵营',p.faction);
        var hasStar=!!(s.rank||s.value||s.level||s.guardians||s.current||s.next),prog=(Number(s.percent)>=0)?('<div class="progress"><i style="width:'+Math.max(0,Math.min(100,Number(s.percent)))+'%"></i></div><div class="progressText">'+(s.current?ni

((/(role|character)/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[z].name);if(fid){out[z].id=fid;byId[fid]=z;}}
    }

    var cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600";
    for(var q=0;q<out.length;q++)if(!out[q].image)out[q].image=cover;
    return out;
}

function qfRoleMetaB22(raw){
    var profile={gender:"",birthday:"",constellation:"",age:"",identity:"",faction:""};
    var star={rank:"",value:"",level:"",guardians:"",current:0,next:0,percent:-1};
    if(!raw||typeof raw!=="object")return {profile:profile,star:star};
    var seen=[];
    function clean(v){
        if(v===undefined||v===null)return "";
        if(typeof v==="number"){if(!isFinite(v))return "";return String(v);}
        if(typeof v==="boolean")return v?"是":"否";
        if(typeof v!=="string")return "";
        var s=qfRolePlainB61(v);return s&&s!=="0"?s:"";
    }
    function primitive(o,names){
        if(!o||typeof o!=="object"||Array.isArray(o))return "";
        var map={};for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k))map[String(k).toLowerCase()]=k;
        for(var i=0;i<names.length;i++){
            var rk=map[String(names[i]).toLowerCase()];if(rk===undefined)continue;
            var x=o[rk];if(x!==null&&typeof x!=="object"){var s=clean(x);if(s)return s;}
        }
        return "";
    }
    function num(o,names){var s=primitive(o,names);if(!s)return 0;var n=Number(String(s).replace(/,/g,""));return isFinite(n)?n:0;}
    function walk(o,d){
        if(!o||typeof o!=="object"||d>5||seen.indexOf(o)>=0)return;seen.push(o);
        if(!profile.gender){var g=primitive(o,["GenderName","SexName","Gender","Sex"]);if(g&&!/^\d+$/.test(g))profile.gender=g;}
        if(!profile.birthday)profile.birthday=primitive(o,["Birthday","BirthDay","BirthDate","DateOfBirth"]);
        if(!profile.constellation)profile.constellation=primitive(o,["Constellation","StarSign","Zodiac"]);
        if(!profile.age){var a=primitive(o,["Age"]);if(a&&!/^0$/.test(a))profile.age=a;}
        if(!profile.identity)profile.identity=primitive(o,["IdentityName","RoleIdentity","Occupation","Career","Profession"]);
        if(!profile.faction)profile.faction=primitive(o,["FactionName","CampName","OrganizationName","ForceName"]);
        if(!star.rank)star.rank=primitive(o,["GuardianRank","GuardRank","RankNo","Rank","Ranking","SupportRank"]);
        if(!star.value)star.value=primitive(o,["StarValue","StarLightValue","GloryValue","SupportValue","RelationshipValue","RelationValue"]);
        if(!star.level)star.level=primitive(o,["StarLevelName","LevelName","RelationshipLevelName","RelationLevelName","GuardianLevelName"]);
        if(!star.guardians)star.guardians=primitive(o,["GuardianCount","GuardCount","SupporterCount","FansCount"]);
        if(!star.current)star.current=num(o,["CurrentValue","CurrentExp","CurrentScore","CurrentStarValue","CurValue"]);
        if(!star.next)star.next=num(o,["NextValue","NextExp","NextScore","NextStarValue","UpgradeValue","TargetValue"]);
        if(Array.isArray(o)){for(var i=0;i<o.length&&i<30;i++)walk(o[i],d+1);return;}
        for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k)){var x=o[k];if(x&&typeof x==="object")walk(x,d+1);}
    }
    walk(raw,0);
    if(star.current>0&&star.next>star.current)star.percent=Math.max(0,Math.min(100,Math.round(star.current*100/star.next)));
    return {profile:profile,star:star};
}

function qfRoleBookName2940(root,bookObj){
    try{
        var n=String(bookObj&&bookObj.name||"").trim();
        if(n)return n;
    }catch(e0){}

    var d=(root&&(root.Data||root.data))||root||{};
    var bi=d.BaseBookInfo||d.baseBookInfo||d.BookInfo||d.bookInfo||{};
    return String(
        bi.BookName||bi.bookName||
        bi.Name||bi.name||
        d.BookName||d.bookName||
        "书籍信息"
    ).trim()||"书籍信息";
}

function qfRoleHtml2940(root,bid,bookObj,roles){
    var bookName=qfRoleBookName2940(root,bookObj),cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600",cards="";
    function v(x){return qfRoleEsc2940(String(x==null?"":x));}
    function nice(x){var n=Number(x);return isFinite(n)&&n>0?qfRolePrettyNum2943(n):v(x);}
    for(var i=0;i<roles.length;i++){
        var r=roles[i]||{},meta=(r.profile||r.star)?{profile:r.profile||{},star:r.star||{}}:qfRoleMetaB22(r.raw||{}),p=meta.profile||{},s=meta.star||{},tags="",rows="";
        for(var k=0;k<(r.tags||[]).length;k++){var t=r.tags[k]||{};tags+='<span class="roleTag">'+v(t.name)+(t.likes>0?'<b> · '+v(qfRolePrettyNum2943(t.likes))+'</b>':'')+'</span>';}
        function row(label,val){if(val)rows+='<div class="kv"><span>'+label+'</span><b>'+v(val)+'</b></div>';}
        row('性别',p.gender);row('生日',p.birthday);row('星座',p.constellation);row('年龄',p.age);row('身份',p.identity);row('阵营',p.faction);
        var hasStar=!!(s.rank||s.value||s.level||s.guardians||s.current||s.next),prog=(Number(s.percent)>=0)?('<div class="progress"><i style="width:'+Math.max(0,Math.min(100,Number(s.percent)))+'%"></i></div><div class="progressText">'+(s.current?nice(s.current):'

/.test(lk)&&/id$/.test(lk))||(/^id(role|character)/.test(lk)))){
                var y=cleanId(o[k]);if(y)return y;
            }
        }
        if(allowGeneric){var z=cleanId(o.Id||o.id);if(z)return z;}
        return "";
    }
    function nestedRoleId(o,depth){
        if(!o||typeof o!=="object"||depth>3)return "";
        var id=directRoleId(o,false);if(id)return id;
        var pref=["Role","role","RoleInfo","roleInfo","TopRoleInfo","topRoleInfo","BookRole","bookRole","Character","character"];
        for(var i=0;i<pref.length;i++){
            var v=o[pref[i]];
            if(v&&typeof v==="object"){
                id=directRoleId(v,true)||nestedRoleId(v,depth+1);if(id)return id;
            }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[z].name);if(fid){out[z].id=fid;byId[fid]=z;}}
    }

    var cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600";
    for(var q=0;q<out.length;q++)if(!out[q].image)out[q].image=cover;
    return out;
}

function qfRoleMetaB22(raw){
    var profile={gender:"",birthday:"",constellation:"",age:"",identity:"",faction:""};
    var star={rank:"",value:"",level:"",guardians:"",current:0,next:0,percent:-1};
    if(!raw||typeof raw!=="object")return {profile:profile,star:star};
    var seen=[];
    function clean(v){
        if(v===undefined||v===null)return "";
        if(typeof v==="number"){if(!isFinite(v))return "";return String(v);}
        if(typeof v==="boolean")return v?"是":"否";
        if(typeof v!=="string")return "";
        var s=qfRolePlainB61(v);return s&&s!=="0"?s:"";
    }
    function primitive(o,names){
        if(!o||typeof o!=="object"||Array.isArray(o))return "";
        var map={};for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k))map[String(k).toLowerCase()]=k;
        for(var i=0;i<names.length;i++){
            var rk=map[String(names[i]).toLowerCase()];if(rk===undefined)continue;
            var x=o[rk];if(x!==null&&typeof x!=="object"){var s=clean(x);if(s)return s;}
        }
        return "";
    }
    function num(o,names){var s=primitive(o,names);if(!s)return 0;var n=Number(String(s).replace(/,/g,""));return isFinite(n)?n:0;}
    function walk(o,d){
        if(!o||typeof o!=="object"||d>5||seen.indexOf(o)>=0)return;seen.push(o);
        if(!profile.gender){var g=primitive(o,["GenderName","SexName","Gender","Sex"]);if(g&&!/^\d+$/.test(g))profile.gender=g;}
        if(!profile.birthday)profile.birthday=primitive(o,["Birthday","BirthDay","BirthDate","DateOfBirth"]);
        if(!profile.constellation)profile.constellation=primitive(o,["Constellation","StarSign","Zodiac"]);
        if(!profile.age){var a=primitive(o,["Age"]);if(a&&!/^0$/.test(a))profile.age=a;}
        if(!profile.identity)profile.identity=primitive(o,["IdentityName","RoleIdentity","Occupation","Career","Profession"]);
        if(!profile.faction)profile.faction=primitive(o,["FactionName","CampName","OrganizationName","ForceName"]);
        if(!star.rank)star.rank=primitive(o,["GuardianRank","GuardRank","RankNo","Rank","Ranking","SupportRank"]);
        if(!star.value)star.value=primitive(o,["StarValue","StarLightValue","GloryValue","SupportValue","RelationshipValue","RelationValue"]);
        if(!star.level)star.level=primitive(o,["StarLevelName","LevelName","RelationshipLevelName","RelationLevelName","GuardianLevelName"]);
        if(!star.guardians)star.guardians=primitive(o,["GuardianCount","GuardCount","SupporterCount","FansCount"]);
        if(!star.current)star.current=num(o,["CurrentValue","CurrentExp","CurrentScore","CurrentStarValue","CurValue"]);
        if(!star.next)star.next=num(o,["NextValue","NextExp","NextScore","NextStarValue","UpgradeValue","TargetValue"]);
        if(Array.isArray(o)){for(var i=0;i<o.length&&i<30;i++)walk(o[i],d+1);return;}
        for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k)){var x=o[k];if(x&&typeof x==="object")walk(x,d+1);}
    }
    walk(raw,0);
    if(star.current>0&&star.next>star.current)star.percent=Math.max(0,Math.min(100,Math.round(star.current*100/star.next)));
    return {profile:profile,star:star};
}

function qfRoleBookName2940(root,bookObj){
    try{
        var n=String(bookObj&&bookObj.name||"").trim();
        if(n)return n;
    }catch(e0){}

    var d=(root&&(root.Data||root.data))||root||{};
    var bi=d.BaseBookInfo||d.baseBookInfo||d.BookInfo||d.bookInfo||{};
    return String(
        bi.BookName||bi.bookName||
        bi.Name||bi.name||
        d.BookName||d.bookName||
        "书籍信息"
    ).trim()||"书籍信息";
}

function qfRoleHtml2940(root,bid,bookObj,roles){
    var bookName=qfRoleBookName2940(root,bookObj),cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600",cards="";
    function v(x){return qfRoleEsc2940(String(x==null?"":x));}
    function nice(x){var n=Number(x);return isFinite(n)&&n>0?qfRolePrettyNum2943(n):v(x);}
    for(var i=0;i<roles.length;i++){
        var r=roles[i]||{},meta=(r.profile||r.star)?{profile:r.profile||{},star:r.star||{}}:qfRoleMetaB22(r.raw||{}),p=meta.profile||{},s=meta.star||{},tags="",rows="";
        for(var k=0;k<(r.tags||[]).length;k++){var t=r.tags[k]||{};tags+='<span class="roleTag">'+v(t.name)+(t.likes>0?'<b> · '+v(qfRolePrettyNum2943(t.likes))+'</b>':'')+'</span>';}
        function row(label,val){if(val)rows+='<div class="kv"><span>'+label+'</span><b>'+v(val)+'</b></div>';}
        row('性别',p.gender);row('生日',p.birthday);row('星座',p.constellation);row('年龄',p.age);row('身份',p.identity);row('阵营',p.faction);
        var hasStar=!!(s.rank||s.value||s.level||s.guardians||s.current||s.next),prog=(Number(s.percent)>=0)?('<div class="progress"><i style="width:'+Math.max(0,Math.min(100,Number(s.percent)))+'%"></i></div><div class="progressText">'+(s.current?nice(s.current):'')+(s.next?' / '+ni

     }
        }
        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[z].name);if(fid){out[z].id=fid;byId[fid]=z;}}
    }

    var cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600";
    for(var q=0;q<out.length;q++)if(!out[q].image)out[q].image=cover;
    return out;
}

function qfRoleMetaB22(raw){
    var profile={gender:"",birthday:"",constellation:"",age:"",identity:"",faction:""};
    var star={rank:"",value:"",level:"",guardians:"",current:0,next:0,percent:-1};
    if(!raw||typeof raw!=="object")return {profile:profile,star:star};
    var seen=[];
    function clean(v){
        if(v===undefined||v===null)return "";
        if(typeof v==="number"){if(!isFinite(v))return "";return String(v);}
        if(typeof v==="boolean")return v?"是":"否";
        if(typeof v!=="string")return "";
        var s=qfRolePlainB61(v);return s&&s!=="0"?s:"";
    }
    function primitive(o,names){
        if(!o||typeof o!=="object"||Array.isArray(o))return "";
        var map={};for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k))map[String(k).toLowerCase()]=k;
        for(var i=0;i<names.length;i++){
            var rk=map[String(names[i]).toLowerCase()];if(rk===undefined)continue;
            var x=o[rk];if(x!==null&&typeof x!=="object"){var s=clean(x);if(s)return s;}
        }
        return "";
    }
    function num(o,names){var s=primitive(o,names);if(!s)return 0;var n=Number(String(s).replace(/,/g,""));return isFinite(n)?n:0;}
    function walk(o,d){
        if(!o||typeof o!=="object"||d>5||seen.indexOf(o)>=0)return;seen.push(o);
        if(!profile.gender){var g=primitive(o,["GenderName","SexName","Gender","Sex"]);if(g&&!/^\d+$/.test(g))profile.gender=g;}
        if(!profile.birthday)profile.birthday=primitive(o,["Birthday","BirthDay","BirthDate","DateOfBirth"]);
        if(!profile.constellation)profile.constellation=primitive(o,["Constellation","StarSign","Zodiac"]);
        if(!profile.age){var a=primitive(o,["Age"]);if(a&&!/^0$/.test(a))profile.age=a;}
        if(!profile.identity)profile.identity=primitive(o,["IdentityName","RoleIdentity","Occupation","Career","Profession"]);
        if(!profile.faction)profile.faction=primitive(o,["FactionName","CampName","OrganizationName","ForceName"]);
        if(!star.rank)star.rank=primitive(o,["GuardianRank","GuardRank","RankNo","Rank","Ranking","SupportRank"]);
        if(!star.value)star.value=primitive(o,["StarValue","StarLightValue","GloryValue","SupportValue","RelationshipValue","RelationValue"]);
        if(!star.level)star.level=primitive(o,["StarLevelName","LevelName","RelationshipLevelName","RelationLevelName","GuardianLevelName"]);
        if(!star.guardians)star.guardians=primitive(o,["GuardianCount","GuardCount","SupporterCount","FansCount"]);
        if(!star.current)star.current=num(o,["CurrentValue","CurrentExp","CurrentScore","CurrentStarValue","CurValue"]);
        if(!star.next)star.next=num(o,["NextValue","NextExp","NextScore","NextStarValue","UpgradeValue","TargetValue"]);
        if(Array.isArray(o)){for(var i=0;i<o.length&&i<30;i++)walk(o[i],d+1);return;}
        for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k)){var x=o[k];if(x&&typeof x==="object")walk(x,d+1);}
    }
    walk(raw,0);
    if(star.current>0&&star.next>star.current)star.percent=Math.max(0,Math.min(100,Math.round(star.current*100/star.next)));
    return {profile:profile,star:star};
}

function qfRoleBookName2940(root,bookObj){
    try{
        var n=String(bookObj&&bookObj.name||"").trim();
        if(n)return n;
    }catch(e0){}

    var d=(root&&(root.Data||root.data))||root||{};
    var bi=d.BaseBookInfo||d.baseBookInfo||d.BookInfo||d.bookInfo||{};
    return String(
        bi.BookName||bi.bookName||
        bi.Name||bi.name||
        d.BookName||d.bookName||
        "书籍信息"
    ).trim()||"书籍信息";
}

function qfRoleHtml2940(root,bid,bookObj,roles){
    var bookName=qfRoleBookName2940(root,bookObj),cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600",cards="";
    function v(x){return qfRoleEsc2940(String(x==null?"":x));}
    function nice(x){var n=Number(x);return isFinite(n)&&n>0?qfRolePrettyNum2943(n):v(x);}
    for(var i=0;i<roles.length;i++){
        var r=roles[i]||{},meta=(r.profile||r.star)?{profile:r.profile||{},star:r.star||{}}:qfRoleMetaB22(r.raw||{}),p=meta.profile||{},s=meta.star||{},tags="",rows="";
        for(var k=0;k<(r.tags||[]).length;k++){var t=r.tags[k]||{};tags+='<span class="roleTag">'+v(t.name)+(t.likes>0?'<b> · '+v(qfRolePrettyNum2943(t.likes))+'</b>':'')+'</span>';}
        function row(label,val){if(val)rows+='<div class="kv"><span>'+label+'</span><b>'+v(val)+'</b></div>';}
        row('性别',p.gender);row('生日',p.birthday);row('星座',p.constellation);row('年龄',p.age);row('身份',p.identity);row('阵营',p.faction);
        var hasStar=!!(s.rank||s.value||s.level||s.guardians||s.current||s.next),prog=(Number(s.percent)>=0)?('<div class="progress"><i style="width:'+Math.max(0,Math.min(100,Number(s.percent)))+'%"></i></div><div class="progressText">'+(s.current?nice(s.current):'')+(s.next?' / '+nice(s.next):'')+'</div>'):'',img=v(r.image||cover),imgAttr=i===0?('src="'+img+'" loading="eager" fetchpriority="high"'):('src="'+v(cover)+'" data-src="'+img+'" loading="lazy" fetchpriority="low"'),desc=String(r.desc||'');
        if(desc==='角色太过神秘还没有简介')desc='';
        cards+='<section class="slide'+(i===0?' active':'')+'"><article class="card" data-bg="'+img+'" data-roleid="'+v(r.id||'')+'">'+
        '<div class="photo"><img class="roleImg" '+imgAttr+' data-fallback="'+v(cover)+'" decoding="async" referrerpolicy="no-referrer"><div class="photoShade"></div><div class="heroMeta"><div class="heroLine"><div class="roleName">'+v(r.name)+'</div>'+(r.position?'<span class="position">'+v(r.position)+'</

        for(var k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[z].name);if(fid){out[z].id=fid;byId[fid]=z;}}
    }

    var cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600";
    for(var q=0;q<out.length;q++)if(!out[q].image)out[q].image=cover;
    return out;
}

function qfRoleMetaB22(raw){
    var profile={gender:"",birthday:"",constellation:"",age:"",identity:"",faction:""};
    var star={rank:"",value:"",level:"",guardians:"",current:0,next:0,percent:-1};
    if(!raw||typeof raw!=="object")return {profile:profile,star:star};
    var seen=[];
    function clean(v){
        if(v===undefined||v===null)return "";
        if(typeof v==="number"){if(!isFinite(v))return "";return String(v);}
        if(typeof v==="boolean")return v?"是":"否";
        if(typeof v!=="string")return "";
        var s=qfRolePlainB61(v);return s&&s!=="0"?s:"";
    }
    function primitive(o,names){
        if(!o||typeof o!=="object"||Array.isArray(o))return "";
        var map={};for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k))map[String(k).toLowerCase()]=k;
        for(var i=0;i<names.length;i++){
            var rk=map[String(names[i]).toLowerCase()];if(rk===undefined)continue;
            var x=o[rk];if(x!==null&&typeof x!=="object"){var s=clean(x);if(s)return s;}
        }
        return "";
    }
    function num(o,names){var s=primitive(o,names);if(!s)return 0;var n=Number(String(s).replace(/,/g,""));return isFinite(n)?n:0;}
    function walk(o,d){
        if(!o||typeof o!=="object"||d>5||seen.indexOf(o)>=0)return;seen.push(o);
        if(!profile.gender){var g=primitive(o,["GenderName","SexName","Gender","Sex"]);if(g&&!/^\d+$/.test(g))profile.gender=g;}
        if(!profile.birthday)profile.birthday=primitive(o,["Birthday","BirthDay","BirthDate","DateOfBirth"]);
        if(!profile.constellation)profile.constellation=primitive(o,["Constellation","StarSign","Zodiac"]);
        if(!profile.age){var a=primitive(o,["Age"]);if(a&&!/^0$/.test(a))profile.age=a;}
        if(!profile.identity)profile.identity=primitive(o,["IdentityName","RoleIdentity","Occupation","Career","Profession"]);
        if(!profile.faction)profile.faction=primitive(o,["FactionName","CampName","OrganizationName","ForceName"]);
        if(!star.rank)star.rank=primitive(o,["GuardianRank","GuardRank","RankNo","Rank","Ranking","SupportRank"]);
        if(!star.value)star.value=primitive(o,["StarValue","StarLightValue","GloryValue","SupportValue","RelationshipValue","RelationValue"]);
        if(!star.level)star.level=primitive(o,["StarLevelName","LevelName","RelationshipLevelName","RelationLevelName","GuardianLevelName"]);
        if(!star.guardians)star.guardians=primitive(o,["GuardianCount","GuardCount","SupporterCount","FansCount"]);
        if(!star.current)star.current=num(o,["CurrentValue","CurrentExp","CurrentScore","CurrentStarValue","CurValue"]);
        if(!star.next)star.next=num(o,["NextValue","NextExp","NextScore","NextStarValue","UpgradeValue","TargetValue"]);
        if(Array.isArray(o)){for(var i=0;i<o.length&&i<30;i++)walk(o[i],d+1);return;}
        for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k)){var x=o[k];if(x&&typeof x==="object")walk(x,d+1);}
    }
    walk(raw,0);
    if(star.current>0&&star.next>star.current)star.percent=Math.max(0,Math.min(100,Math.round(star.current*100/star.next)));
    return {profile:profile,star:star};
}

function qfRoleBookName2940(root,bookObj){
    try{
        var n=String(bookObj&&bookObj.name||"").trim();
        if(n)return n;
    }catch(e0){}

    var d=(root&&(root.Data||root.data))||root||{};
    var bi=d.BaseBookInfo||d.baseBookInfo||d.BookInfo||d.bookInfo||{};
    return String(
        bi.BookName||bi.bookName||
        bi.Name||bi.name||
        d.BookName||d.bookName||
        "书籍信息"
    ).trim()||"书籍信息";
}

function qfRoleHtml2940(root,bid,bookObj,roles){
    var bookName=qfRoleBookName2940(root,bookObj),cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600",cards="";
    function v(x){return qfRoleEsc2940(String(x==null?"":x));}
    function nice(x){var n=Number(x);return isFinite(n)&&n>0?qfRolePrettyNum2943(n):v(x);}
    for(var i=0;i<roles.length;i++){
        var r=roles[i]||{},meta=(r.profile||r.star)?{profile:r.profile||{},star:r.star||{}}:qfRoleMetaB22(r.raw||{}),p=meta.profile||{},s=meta.star||{},tags="",rows="";
        for(var k=0;k<(r.tags||[]).length;k++){var t=r.tags[k]||{};tags+='<span class="roleTag">'+v(t.name)+(t.likes>0?'<b> · '+v(qfRolePrettyNum2943(t.likes))+'</b>':'')+'</span>';}
        function row(label,val){if(val)rows+='<div class="kv"><span>'+label+'</span><b>'+v(val)+'</b></div>';}
        row('性别',p.gender);row('生日',p.birthday);row('星座',p.constellation);row('年龄',p.age);row('身份',p.identity);row('阵营',p.faction);
        var hasStar=!!(s.rank||s.value||s.level||s.guardians||s.current||s.next),prog=(Number(s.percent)>=0)?('<div class="progress"><i style="width:'+Math.max(0,Math.min(100,Number(s.percent)))+'%"></i></div><div class="progressText">'+(s.current?nice(s.current):'')+(s.next?' / '+nice(s.next):'')+'</div>'):'',img=v(r.image||cover),imgAttr=i===0?('src="'+img+'" loading="eager" fetchpriority="high"'):('src="'+v(cover)+'" data-src="'+img+'" loading="lazy" fetchpriority="low"'),desc=String(r.desc||'');
        if(desc==='角色太过神秘还没有简介')desc='';
        cards+='<section class="slide'+(i===0?' active':'')+'"><article class="card" data-bg="'+img+'" data-roleid="'+v(r.id||'')+'">'+
        '<div class="photo"><img class="roleImg" '+imgAttr+' data-fallback="'+v(cover)+'" decoding="async" referrerpolicy="no-referrer"><div class="photoShade"></div><div class="heroMeta"><div class="heroLine"><div class="roleName">'+v(r.name)+'</div>'+(r.position?'<span class="position">'+v(r.position)+'</span>':'')+(r.lik

r k in o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)||nestedRoleId(roleObj,0)||nestedRoleId(containerObj,0)||"";
    }
    function normKey(v){return qfRolePlainB61(v).toLowerCase().replace(/\s+/g,"").replace(/[·•:：,，.。!！?？;；"'“”‘’《》〈〉「」『』【】\[\]（）()_\-—–]/g,"");}
    function mergeTags(a,b){
        a=Array.isArray(a)?a:[];b=Array.isArray(b)?b:[];
        var seen={},r=[],i,t;
        for(i=0;i<a.length;i++){t=a[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        for(i=0;i<b.length&&r.length<24;i++){t=b[i]||{};if(t.name&&!seen[t.name]){seen[t.name]=1;r.push(t);}}
        return r;
    }
    function findExisting(name,position,roleId){
        if(roleId&&Object.prototype.hasOwnProperty.call(byId,roleId))return byId[roleId];
        var nk=normKey(name),a=byName[nk]||[],pk=normKey(position);
        for(var i=0;i<a.length;i++){
            var idx=a[i],old=out[idx]||{},op=normKey(old.position||"");
            if(!pk||!op||pk===op)return idx;
        }
        return -1;
    }
    function indexRole(idx){
        var r=out[idx]||{};
        if(r.id)byId[String(r.id)]=idx;
        var nk=normKey(r.name||"");if(nk){if(!byName[nk])byName[nk]=[];if(byName[nk].indexOf(idx)<0)byName[nk].push(idx);}
    }
    function add(roleObj,containerObj){
        if(!roleObj||typeof roleObj!=="object")return;
        var name=qfRolePlainB61(
            roleObj.RoleName||roleObj.roleName||roleObj.CharacterName||roleObj.characterName||roleObj.Name||roleObj.name||""
        );
        if(!name)return;
        var position=qfRolePlainB61(
            roleObj.Position||roleObj.position||roleObj.RolePosition||roleObj.rolePosition||roleObj.Identity||roleObj.identity||roleObj.RoleTypeName||roleObj.roleTypeName||""
        );
        var desc=qfRolePlainB61(
            roleObj.RoleDesc||roleObj.roleDesc||roleObj.CharacterDesc||roleObj.characterDesc||
            roleObj.Description||roleObj.description||roleObj.Introduction||roleObj.introduction||roleObj.Intro||roleObj.intro||roleObj.Desc||roleObj.desc||""
        );
        if(!desc)desc="角色太过神秘还没有简介";
        var likes=qfRoleNum2943(
            roleObj.Likes||roleObj.likes||roleObj.LikeCount||roleObj.likeCount||roleObj.PraiseCount||roleObj.praiseCount||
            roleObj.AgreeAmount||roleObj.agreeAmount||roleObj.HotValue||roleObj.hotValue||0
        );
        var roleId=roleIdOf(roleObj,containerObj),image=qfRoleBestImg2940(roleObj,containerObj),tags=qfRoleTags2940(roleObj,containerObj);
        var idx=findExisting(name,position,roleId);
        if(idx>=0){
            var old=out[idx];
            if(!old.id&&roleId)old.id=roleId;
            if(!old.position&&position)old.position=position;
            if((!old.desc||old.desc==="角色太过神秘还没有简介")&&desc)old.desc=desc;
            else if(desc&&desc!=="角色太过神秘还没有简介"&&String(desc).length>String(old.desc||"").length)old.desc=desc;
            if(likes>Number(old.likes||0))old.likes=likes;
            if(!old.image&&image)old.image=image;
            old.tags=mergeTags(old.tags,tags);
            if(roleObj&&typeof roleObj==="object")old.raw=roleObj;
            indexRole(idx);
            return;
        }
        idx=out.length;
        out.push({id:roleId,name:name,position:position,desc:desc,likes:likes||0,image:image,tags:tags,raw:roleObj});
        indexRole(idx);
    }

    var details=qfRoleArr2940(d.BookLookForDetail||d.bookLookForDetail);
    for(var i=0;i<details.length;i++){
        var x=details[i]||{},mt=Number(x.ModelType||x.modelType||0);
        var r=x.TopRoleInfo||x.topRoleInfo||x.RoleInfo||x.roleInfo||x.CharacterInfo||x.characterInfo;
        if(r&&(mt===3||mt===0))add(r,x);
    }

    function walk(node,depth){
        if(!node||depth>7)return;
        if(Array.isArray(node)){for(var ai=0;ai<node.length;ai++)walk(node[ai],depth+1);return;}
        if(typeof node!=="object")return;
        if(node.TopRoleInfo)add(node.TopRoleInfo,node);
        if(node.topRoleInfo)add(node.topRoleInfo,node);
        if(node.RoleInfo&&(node.RoleName||node.roleName||node.RoleInfo.RoleName||node.RoleInfo.roleName))add(node.RoleInfo,node);
        if(node.roleInfo&&(node.RoleName||node.roleName||node.roleInfo.RoleName||node.roleInfo.roleName))add(node.roleInfo,node);
        var arrays=[
            node.RoleList,node.roleList,node.Roles,node.roles,node.BookRoleList,node.bookRoleList,
            node.RoleCardList,node.roleCardList,node.CharacterList,node.characterList,node.Characters,node.characters,
            node.RoleInfoList,node.roleInfoList,node.RoleDetailList,node.roleDetailList
        ];
        for(var ri=0;ri<arrays.length;ri++){var rs=qfRoleArr2940(arrays[ri]);for(var rj=0;rj<rs.length;rj++)add(rs[rj],rs[rj]);}
        if((node.RoleName||node.roleName||node.CharacterName||node.characterName)&&(
            node.Position||node.position||node.Identity||node.identity||node.RoleDesc||node.roleDesc||
            node.CharacterDesc||node.characterDesc||node.RoleBackGroundImage||node.roleBackGroundImage||node.RoleImage||node.roleImage
        ))add(node,node);
        for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")walk(v,depth+1);}}
    }
    walk(d,0);

    /* 最终只为仍缺 ID 的角色做一次名字邻近补全，避免 BookId/ModelId 被误当 RoleId。 */
    function findIdByName(wanted){
        wanted=normKey(wanted);if(!wanted)return "";
        var found="";
        function scan(node,depth,parent){
            if(found||!node||depth>7)return;
            if(Array.isArray(node)){for(var i=0;i<node.length&&!found;i++)scan(node[i],depth+1,parent);return;}
            if(typeof node!=="object")return;
            var nm=normKey(node.RoleName||node.roleName||node.CharacterName||node.characterName||node.Name||node.name||"");
            if(nm&&nm===wanted){found=roleIdOf(node,parent||node);if(found)return;}
            for(var k in node){if(Object.prototype.hasOwnProperty.call(node,k)){var v=node[k];if(v&&typeof v==="object")scan(v,depth+1,node);if(found)return;}}
        }
        scan(d,0,null);return found;
    }
    for(var z=0;z<out.length;z++){
        if(!out[z].id){var fid=findIdByName(out[z].name);if(fid){out[z].id=fid;byId[fid]=z;}}
    }

    var cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600";
    for(var q=0;q<out.length;q++)if(!out[q].image)out[q].image=cover;
    return out;
}

function qfRoleMetaB22(raw){
    var profile={gender:"",birthday:"",constellation:"",age:"",identity:"",faction:""};
    var star={rank:"",value:"",level:"",guardians:"",current:0,next:0,percent:-1};
    if(!raw||typeof raw!=="object")return {profile:profile,star:star};
    var seen=[];
    function clean(v){
        if(v===undefined||v===null)return "";
        if(typeof v==="number"){if(!isFinite(v))return "";return String(v);}
        if(typeof v==="boolean")return v?"是":"否";
        if(typeof v!=="string")return "";
        var s=qfRolePlainB61(v);return s&&s!=="0"?s:"";
    }
    function primitive(o,names){
        if(!o||typeof o!=="object"||Array.isArray(o))return "";
        var map={};for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k))map[String(k).toLowerCase()]=k;
        for(var i=0;i<names.length;i++){
            var rk=map[String(names[i]).toLowerCase()];if(rk===undefined)continue;
            var x=o[rk];if(x!==null&&typeof x!=="object"){var s=clean(x);if(s)return s;}
        }
        return "";
    }
    function num(o,names){var s=primitive(o,names);if(!s)return 0;var n=Number(String(s).replace(/,/g,""));return isFinite(n)?n:0;}
    function walk(o,d){
        if(!o||typeof o!=="object"||d>5||seen.indexOf(o)>=0)return;seen.push(o);
        if(!profile.gender){var g=primitive(o,["GenderName","SexName","Gender","Sex"]);if(g&&!/^\d+$/.test(g))profile.gender=g;}
        if(!profile.birthday)profile.birthday=primitive(o,["Birthday","BirthDay","BirthDate","DateOfBirth"]);
        if(!profile.constellation)profile.constellation=primitive(o,["Constellation","StarSign","Zodiac"]);
        if(!profile.age){var a=primitive(o,["Age"]);if(a&&!/^0$/.test(a))profile.age=a;}
        if(!profile.identity)profile.identity=primitive(o,["IdentityName","RoleIdentity","Occupation","Career","Profession"]);
        if(!profile.faction)profile.faction=primitive(o,["FactionName","CampName","OrganizationName","ForceName"]);
        if(!star.rank)star.rank=primitive(o,["GuardianRank","GuardRank","RankNo","Rank","Ranking","SupportRank"]);
        if(!star.value)star.value=primitive(o,["StarValue","StarLightValue","GloryValue","SupportValue","RelationshipValue","RelationValue"]);
        if(!star.level)star.level=primitive(o,["StarLevelName","LevelName","RelationshipLevelName","RelationLevelName","GuardianLevelName"]);
        if(!star.guardians)star.guardians=primitive(o,["GuardianCount","GuardCount","SupporterCount","FansCount"]);
        if(!star.current)star.current=num(o,["CurrentValue","CurrentExp","CurrentScore","CurrentStarValue","CurValue"]);
        if(!star.next)star.next=num(o,["NextValue","NextExp","NextScore","NextStarValue","UpgradeValue","TargetValue"]);
        if(Array.isArray(o)){for(var i=0;i<o.length&&i<30;i++)walk(o[i],d+1);return;}
        for(var k in o)if(Object.prototype.hasOwnProperty.call(o,k)){var x=o[k];if(x&&typeof x==="object")walk(x,d+1);}
    }
    walk(raw,0);
    if(star.current>0&&star.next>star.current)star.percent=Math.max(0,Math.min(100,Math.round(star.current*100/star.next)));
    return {profile:profile,star:star};
}

function qfRoleBookName2940(root,bookObj){
    try{
        var n=String(bookObj&&bookObj.name||"").trim();
        if(n)return n;
    }catch(e0){}

    var d=(root&&(root.Data||root.data))||root||{};
    var bi=d.BaseBookInfo||d.baseBookInfo||d.BookInfo||d.bookInfo||{};
    return String(
        bi.BookName||bi.bookName||
        bi.Name||bi.name||
        d.BookName||d.bookName||
        "书籍信息"
    ).trim()||"书籍信息";
}

function qfRoleHtml2940(root,bid,bookObj,roles){
    var bookName=qfRoleBookName2940(root,bookObj),cover="https://bookcover.yuewen.com/qdbimg/349573/"+encodeURIComponent(String(bid))+"/600",cards="";
    function v(x){return qfRoleEsc2940(String(x==null?"":x));}
    function nice(x){var n=Number(x);return isFinite(n)&&n>0?qfRolePrettyNum2943(n):v(x);}
    for(var i=0;i<roles.length;i++){
        var r=roles[i]||{},meta=(r.profile||r.star)?{profile:r.profile||{},star:r.star||{}}:qfRoleMetaB22(r.raw||{}),p=meta.profile||{},s=meta.star||{},tags="",rows="";
        for(var k=0;k<(r.tags||[]).length;k++){var t=r.tags[k]||{};tags+='<span class="roleTag">'+v(t.name)+(t.likes>0?'<b> · '+v(qfRolePrettyNum2943(t.likes))+'</b>':'')+'</span>';}
        function row(label,val){if(val)rows+='<div class="kv"><span>'+label+'</span><b>'+v(val)+'</b></div>';}
        row('性别',p.gender);row('生日',p.birthday);row('星座',p.constellation);row('年龄',p.age);row('身份',p.identity);row('阵营',p.faction);
        var hasStar=!!(s.rank||s.value||s.level||s.guardians||s.current||s.next),prog=(Number(s.percent)>=0)?('<div class="progress"><i style="width:'+Math.max(0,Math.min(100,Number(s.percent)))+'%"></i></div><div class="progressText">'+(s.current?nice(s.current):'')+(s.next?' / '+nice(s.next):'')+'</div>'):'',img=v(r.image||cover),imgAttr=i===0?('src="'+img+'" loading="eager" fetchpriority="high"'):('src="'+v(cover)+'" data-src="'+img+'" loading="lazy" fetchpriority="low"'),desc=String(r.desc||'');
        if(desc==='角色太过神秘还没有简介')desc='';
        cards+='<section class="slide'+(i===0?' active':'')+'"><article class="card" data-bg="'+img+'" data-roleid="'+v(r.id||'')+'">'+
        '<div class="photo"><img class="roleImg" '+imgAttr+' data-fallback="'+v(cover)+'" decoding="async" referrerpolicy="no-referrer"><div class="photoShade"></div><div class="heroMeta"><div class="heroLine"><div class="roleName">'+v(r.name)+'</div>'+(r.position?'<span class="position">'+v(r.position)+'</span>':'')+(r.likes>0?'<span cl

 o){
            if(!Object.prototype.hasOwnProperty.call(o,k))continue;
            var v2=o[k];if(v2&&typeof v2==="object"){id=nestedRoleId(v2,depth+1);if(id)return id;}
        }
        return "";
    }
    function roleIdOf(roleObj,containerObj){
        return directRoleId(roleObj,true)||directRoleId(containerObj,false)|