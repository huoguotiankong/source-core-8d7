name: patch-qidian-beta121b5-module-base64-retired
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Beta5 patch workflow retired. Use the Beta6 runtime-restore path instead."
