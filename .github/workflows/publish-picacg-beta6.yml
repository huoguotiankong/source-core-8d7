name: publish-picacg-beta6
on:
  push:
    branches: [main]
    paths:
      - 'hotfix/picacg-beta6/READY'
permissions:
  contents: write
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Patch and publish Picacg beta6
        run: |
          python tools/publish_picacg_beta6.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add sources/comic/picacg/picacg-beta.json manifest.json subscription/beta.json subscription/comic.json bundles/all-beta.json rss/data/details/beta/picacg.json docs/RELEASE_LOG.md docs/DEVELOPMENT_RULES.md docs/KNOWN_ISSUES.md
          git diff --cached --check
          git commit -m 'feat: publish Picacg beta6 concise naming and interaction order'
          git push origin main
