name: patch-qidian-124b7-catalog-lifecycle
on:
  push:
    branches: [main]
    paths:
      - '.staging/patch-qidian-124b7-catalog-lifecycle.trigger'
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with: {ref: main}
      - name: Build qidian-next 1.2.4-beta7
        run: python3 .staging/run_patch_qidian_124b7_v2.py
      - name: Commit generated Beta
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add \
            sources/novel/qidian-next/qidian-next-beta.json \
            manifest.json \
            subscription/beta.json \
            subscription/novel.json \
            bundles/all-beta.json \
            rss/data/details/beta/qidian-next.json \
            docs/RELEASE_LOG.md \
            docs/sources/qidian-next/PROJECT_HANDOFF.md \
            .staging/qidian-124b7-report.json
          git diff --cached --quiet && { echo 'No generated changes'; exit 1; }
          git commit -m 'qidian beta: 1.2.4-beta7 catalog lifecycle repair'
          git push origin main
